interface:
  display_name: "Xcode Workflow"
  short_description: "Build, test, diagnose, and iterate in Xcode"
  default_prompt: "Use $xcode-workflow to run the appropriate Xcode build, test, diagnostics, feedback, or release-validation workflow."

policy:
  allow_implicit_invocation: true
