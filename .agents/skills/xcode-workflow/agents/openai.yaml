interface:
  display_name: "Xcode Workflow"
  short_description: "Build, diagnose, and verify isolated macOS workflows"
  default_prompt: "Use $xcode-workflow for this build, diagnosis, interaction verification, or human acceptance setup."

policy:
  allow_implicit_invocation: true
