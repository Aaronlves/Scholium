- [ ] Keep this proposal in development until the researcher explicitly approves adoption.
- [ ] Treat the parchment overhaul as frontend-only and preserve all existing product behavior, workflows, state meanings, commands, and interaction routes.
- [ ] Preserve Triptych structure, Markdown and YAML fidelity, autosave, indexing, checkpoints, conflict recovery, generated-state locations, and researcher notes without behavioral change.
- [ ] Use the supplied paper photograph solely as a reference for background material, not for layout or typography.
- [ ] Draw broader visual inspiration from the photograph’s restrained editorial rules, calm negative space, and quiet paper hierarchy without copying its page composition.
- [ ] Define Scholium’s intended emotional character as a calm, immersive digital-parchment research environment.
- [ ] Use clean digital parchment rather than visibly distressed, stained, folded, torn, or historically aged paper.
- [ ] Define the visual differences between the reference material and Scholium’s research workflow.
- [ ] Require any view-layer refactoring to preserve current behavior and pass existing behavioral tests.
- [ ] Confirm that the Library is a flush, full-height navigation rail rather than a floating panel.
- [ ] Define the document canvas as opaque clean digital parchment rather than frosted glass or translucent paper.
- [ ] Use authored light/dark parchment artwork for the atmospheric background without desktop translucency or a gradient-only substitute.
- [ ] Preserve the native macOS window frame, titlebar, traffic lights, toolbar, and separate system menu bar.
- [ ] Decide whether the development build is installed alongside or replaces the current Scholium app.
- [ ] Define the design-system layering model: foundations, semantic variables, recipes, components, patterns, and states.
- [ ] Freeze the shared variable architecture at eight families: Color, Typography, Surfaces, Elevation, Boundaries, Metrics, Motion, and Document Rhythm.
- [ ] Keep authored paper texture, gradient blur, vibrancy, and accessibility fallbacks inside environment-aware surface recipes rather than creating generic texture, blur, gradient, or opacity scales.
- [ ] Keep selection, focus, disabled, pressed, hover, and inactive-window rendering native wherever a standard control owns the state.
- [ ] Keep stale, conflict, agent authorship, Human Review, qualification, and row status as composite presentations rather than additional variable families.
- [ ] Audit every existing design variable and classify it as retained, renamed, replaced, provisional, or removed.
- [ ] Preserve the existing semantic Color roles for document, navigation, surface, raised surface, text hierarchy, separators, accent, status, authorship, and Connections.
- [ ] Add no generic paper, ink, opacity, gradient, disabled, selection, or numbered color scales.
- [ ] Reuse the existing parchment palette through semantic roles and add an atmosphere-specific Color role only if the light/dark whole-window proofs require a value distinct from navigation and panel fallbacks.
- [ ] Preserve `ScholiumInterfaceTypography.identity`, `sectionTitle`, `rowTitle`, and `metadata` and use `metadata` for compact ink-like row status.
- [ ] Preserve document typography roles `body`, `heading(level:)`, `exactSource`, `code`, `diff`, and `revisionIdentity`.
- [ ] Preserve Alegreya for prose, native macOS system typography for interface chrome, and Victor Mono in production pending the existing mono comparison.
- [ ] Define `ScholiumSurfaceRole` as `windowAtmosphere`, `navigation`, `document`, `inspector`, `floatingControl`, `boundedPanel`, `searchOverlay`, and `denseEvidence`.
- [ ] Resolve `windowAtmosphere` as authored opaque light/dark digital parchment with no runtime animation or semantic content.
- [ ] Resolve `navigation` and `inspector` as mirrored gradient-blur vellum recipes over the shared atmosphere, with opaque semantic fallbacks.
- [ ] Resolve `document` and `denseEvidence` as opaque paper surfaces with the document receiving the quietest visible texture.
- [ ] Resolve `floatingControl` and `searchOverlay` through restrained regular Liquid Glass and `boundedPanel` through regular material with an opaque fallback.
- [ ] Define material recipes for normal, dark, Increase Contrast, Reduce Transparency, and inactive-window conditions.
- [ ] Define gradient-blur material recipes for the full-height Library sidebar and trailing research inspector over the shared parchment atmosphere.
- [ ] Determine the sidebar and inspector blur direction, falloff, density, and edge transition through whole-window visual proofs.
- [ ] Keep text, selection, symbols, focus, and state presentation legible at every point along each gradient blur.
- [ ] Replace gradient blur with opaque semantic fallbacks under Reduce Transparency and strengthen structural separation under Increase Contrast.
- [ ] Preserve `ScholiumBoundaryRole.structuralDivider`, `subtleBoundary`, and `floatingBoundary`.
- [ ] Use `structuralDivider` only between major window regions, `subtleBoundary` for restrained editorial section rules, and `floatingBoundary` only when a floating surface needs a visible edge.
- [ ] Define a clean, thin structural-rule grammar for partitioning major interface areas and subordinate sections.
- [ ] Use structural rules selectively rather than outlining every component, row, or control.
- [ ] Make structural rules adapt across light, dark, Increase Contrast, Reduce Transparency, and inactive-window conditions.
- [ ] Replace the navigation-only `triptychEdge` elevation name with one `peripheralPanelEdge` role that mirrors correctly for the Library and inspector.
- [ ] Preserve elevation roles `floatingControl`, `boundedPanel`, and `searchOverlay` and restrict every custom shadow to a genuine layer change.
- [ ] Add no generic shadow, blur-radius, opacity, or depth scale.
- [ ] Keep shapes and corner radii component- or feature-owned rather than creating a ninth shared variable family.
- [ ] Remove `ScholiumMetrics.Navigation.panelInset` and `panelCornerRadius` after the floating Library treatment is removed.
- [ ] Preserve feature-scoped Metrics, `Accessibility.preferredCustomTarget = 28`, `Accessibility.minimumCustomTarget = 20`, and `ContextSurface.controlHeight = 40`.
- [ ] Let native list density and standard controls own ordinary row heights, padding, corner radii, and hit targets unless visual testing proves a Scholium-owned exception is necessary.
- [ ] Preserve the existing semantic Motion functions and add no duration scale, animated paper, parallax, or moving grain.
- [ ] Preserve the current renderer-aware Document Rhythm variables as provisional until Read and Live Preview pass side-by-side visual review.
- [ ] Define domain iconography and retain direct SF Symbols for standard commands.
- [ ] Define composite presentations for stale, conflict, agent authorship, review, and Connection states.
- [ ] Define the immersive window-shell component.
- [ ] Define the full-height Library navigation component.
- [ ] Define compact note rows with the title as the only content preview and remove body-excerpt previews from ordinary Library rows.
- [ ] Keep ordinary note titles to one compact line with predictable truncation and an accessible route to the complete title.
- [ ] Define one restrained trailing status slot for each note row using only existing role-valid property, Human Review, qualification, lifecycle, conflict, or recovery states.
- [ ] Define status precedence so conflict and recovery states supersede ordinary role status without displaying several competing badges.
- [ ] Present row status as short ink-like metadata with redundant text and symbol rather than a colorful chip or color-only dot.
- [ ] Define the document-canvas component for Read, Live Preview, and Source.
- [ ] Define the research-inspector component and its relationship to the window atmosphere.
- [ ] Define the document-context component for metadata and Properties.
- [ ] Remove the persistent Tags block from the research inspector and keep the inspector focused on Connections, research context, diagnostics, provenance, and Attention.
- [ ] Define the compact Properties disclosure as a role-aware summary that exposes only the highest-priority configured fields before expansion.
- [ ] Define the default compact property summary for Analyses, Topics, and Works without inventing a generic cross-role lifecycle.
- [ ] Keep complete tag viewing and editing inside expanded Properties and use a single-line count or truncated summary when tags must appear in the compact disclosure.
- [ ] Define how the compact Properties summary shortens at constrained widths before hiding essential role or research-state information.
- [ ] Define Search, floating command, bounded panel, dense evidence, lifecycle, loading, error, conflict, and recovery components.
- [ ] Define selection, hover, focus, keyboard-focus, disabled, and inactive-window behavior for every custom component.
- [ ] Define component behavior at 1380-, 1080-, 900-, and minimum-width windows.
- [ ] Define component behavior at normal and 200% document text sizes.
- [ ] Create a preview catalog that compares every semantic recipe and component across required appearances and accessibility environments.
- [ ] Produce a light and dark whole-window visual proof before freezing the immersive-shell recipes.
- [ ] Obtain researcher approval for the design-system contract before changing production interface code.
- [ ] Record approved stable decisions in the Design Handbook before declaring implementation complete.
- [ ] Implement the approved variables, recipes, and components without replacing the native NavigationSplitView.
- [ ] Migrate repeated literals only after their semantic owner is approved.
- [ ] Remove the floating Library treatment after the flush navigation component is implemented.
- [ ] Preserve native sidebar, toolbar, menu, keyboard, pointer, focus, accessibility, and full-screen behavior.
- [ ] Preserve opaque or sufficiently legible source, diff, diagnostic, conflict, and exact-evidence presentations.
- [ ] Verify empty, loading, open-document, editing, Search, Properties, inspector, conflict, error, and recovery states.
- [ ] Verify light, dark, Increase Contrast, Reduce Transparency, Reduce Motion, and inactive-window behavior.
- [ ] Verify keyboard-only, pointer, focus-ring, VoiceOver, Voice Control, and 200% text-size routes where deterministically testable.
- [ ] Test usability through the isolated QA app and disposable nonprivate Triptych fixtures.
- [ ] Complete a human usability journey for reading, editing, searching, reviewing, Dialogue, Critique, checkpoints, and recovery.
- [ ] Record which usability findings are automated, exploratory, or still manual.
- [ ] Establish byte-for-byte fixture baselines before opening a Triptych with the development build.
- [ ] Verify that launch, registration, indexing, reading, and application upgrade do not modify Markdown notes.
- [ ] Verify that intentional edits preserve BOM, newline style, comments, unknown YAML, ordering, quoting, multiline values, and final newlines outside changed ranges.
- [ ] Verify that indexes, caches, reviews, Dialogue records, checkpoints, and other generated state remain outside research notes and in their approved locations.
- [ ] Verify stale-fingerprint, external-edit, conflict, interrupted-save, and recovery behavior on disposable Triptych copies.
- [ ] Compare complete Triptych file hashes before and after installation and upgrade journeys.
- [ ] Run focused Swift tests, WebEditor tests, isolated QA journeys, and the complete repository verifier.
- [ ] Build and strictly verify a usable development application artifact.
- [ ] Keep the development artifact isolated from the current installed app and production preferences unless replacement is explicitly approved.
- [ ] Smoke-test the built artifact without opening a private research vault.
- [ ] Report the artifact path, signing state, tested fixture, verified safety invariants, and remaining manual acceptance work.
- [ ] Decide whether to revise, continue developing, adopt, or reject the immersive interface after visual and usability review.
