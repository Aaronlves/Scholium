# Scholium Implementation Status

**Audited:** 2026-07-22
**Target authority:** [SCHOLIUM_SPEC.md](SCHOLIUM_SPEC.md)
**Scope:** current reachability, verification evidence, migration debt, and open
acceptance only. This ledger cannot redefine the target specification.

## Reachable target behavior

| Area | Current reachable boundary |
| --- | --- |
| Triptych and storage | Multiple registered Triptychs use independently chosen Analyses, Topics, and Works; each window belongs to one Triptych. Portable control state stays beside Works in `.scholium`; machine/derived state stays in Application Support. The atomic v2 registry does not import the retired single-workspace registry. |
| Exact source | `NoteDocument`, targeted YAML edits, coordinated autosave/readback, conflict handling, typed Markdown paths, volume-aware collision checks, and descriptor-relative symlink rejection preserve authoritative Markdown. Existing-file commits require displaced-byte-preserving swap and exact two-sided readback; unsupported volumes fail closed. Current roles re-encode canonically; malformed or unknown current fields fail without rewriting source. |
| Lifecycle and deletion | Create, duplicate, move/rename, Set Aside, Trash, exact-path Put Back, and permanent deletion route through `VaultRepository` and `VaultMutationCoordinator`. Deletion removes the note, current Critique, machine-local prewrite recovery, identity, records, and affected checkpoints. A durable higher-level deletion journal restores pre-commit state or completes post-commit privacy cleanup; concurrent path recreation is preserved for explicit recovery. |
| Move and import | Confirmed moves preflight destination and incoming-link revisions and rewrite only identically resolved links. Stable identity/records follow confirmed moves or reconciled external renames; ambiguity requires confirmation. Unclassified imports remain editable and classify into any vault. Multi-file failure rolls back revision-safe exact bytes or produces a visible external-recovery record. |
| Review and Critique | Analyses/Topics have fingerprint-bound Human Review, required concise Review Notes, drafts, and anchored Comments; no whole-note Comment fallback exists. A Work has at most one attributed current Critique under `Works/Critiques`, read-only in Scholium but externally editable. Findings bind the target fingerprint and navigate only unambiguous passages. Earlier-round chronology and Accept/Reject/Rebut dispositions remain unimplemented. |
| Research Functions | One per-window Inspector exposes Overview, Connections, and role-valid Functions. Document has no bottom Research Strip. Functions launch the existing typed panels, which fix Target and choose read-only Materials, scope, Comments, and Fidelity checks; menu/keyboard routes remain independent of Inspector visibility or mode. Dialogue is nonmutating unless promoted. Develop, Revise, Manuscript, and Critique checkpoint; Human Review and Fidelity do not. |
| Conditional methods and Fidelity | Same-run preflight persists the normal run, checkpoint, and Dialogue/Critique record; explicit resource selection may be empty without changing identities. Write runs record the exact final revision as Awaiting Fidelity and accept only a validated independent child. Direct outcomes are rejected; missing checks are Unverified; changed inputs become stale; exact evidence keys reuse committed audits without implying agent execution. |
| Recovery and chronology | Self-contained external checkpoints support ten automatic snapshots, unlimited manual retention, comparison, selective/full restore, and Finder access; pre-restore safety creation cannot evict its source. Core also keeps a non-delivery SQLite/immutable-object prewrite ledger with v1 preservation, corruption quarantine/rebuild, remap, retention, and deletion tombstones. The obsolete Note History versions/restore API is removed; checkpoint restore is the only visible restore mechanism. Research Record remains separate chronology/provenance. |
| Application and CLI | GUI and CLI share Application capabilities and trust rules. Each CLI invocation uses one snapshot runtime; existing-note mutations require current SHA-256. App/CLI import only Contracts and Application, Core remains internal, one live app runtime has one accepted event subscription, and per-window controllers own independent presentation state. |
| Connections, Search, and Attention | One workspace `GraphSnapshot` owns Vector-Link v1 resolution; neutral/transitive paths never become evidence and legacy syntax remains exact with diagnostics. Shared SQLite FTS5 covers GUI/CLI and Unicode/CJK. Attention is limited to specified structural, currency, connection, reliance, metadata, and identity conditions; dismissals are Triptych-local and deadlines machine-local. |
| Editor | CodeMirror Source/Live and sanitized Read share one checked semantic projection and the same callout, table, footnote, math, preview, font, and presentation contracts. Generated callout roles are accessibility-only; supplied titles inherit role styling. One retained Host keeps Read and one CodeMirror state mounted. Bridge v4 preserves exact source, identity, recovery, focus, and scroll while coalescing selection reports. Automated TypeScript, Swift, real-WKWebView, and isolated QA cover implemented semantics; human visual, assistive-technology, IME, retained-memory, and sustained-performance acceptance remain open. |
| Properties and Zotero | Per-vault profiles control order, editable allowlists, and disclosure; `research_unit` has a role-neutral Research Status editor. Zotero is localhost-only/read-only, refuses ambiguous identity, shows only the current Analysis source or outgoing-linked Analysis sources, and offers only Open in Zotero—never attachment access. |
| Product Skills | Protected typed resources reject traversal/symlink escape; revisions cover bounded resources and assembly records only loaded conditional resources. Exactly five Workflow packages remain; Dialogue is System infrastructure and Human Review has no Skill. Source Analyzer, APA 7 verification, and Prose Control are opt-in copy-on-adoption Researcher Skills, not automatic or universal authorities. |
| Research Guidance | Compatible Triptych Skills may replace or supplement a function and contribute exact Practices through revision-checked bindings; Functions mode sees no package IDs. Citation Fidelity requires an explicit capability/style binding. Opted-in local Skills support guarded proposal, comparison, validation, attributed evaluation, atomic replacement, snapshots, and recovery; bundled packages remain immutable. |
| Dialogue and MCP | Dialogue persists per-Triptych defaults and an immutable request-time response contract: Academic Outcome is mandatory and five modules optional; Practices cannot change scope or permissions. External Zotero MCP remains separate from the built-in reader; only explicit `--probe` performs its read-only initialization lifecycle. |

## Adopted boundaries with remaining acceptance

| Boundary | Implemented evidence | Still open |
| --- | --- | --- |
| Localization | English default; complete zh-Hans string catalogs, compiler-synchronized formats, coverage/placeholder/state/compilation validation, and outer-bundle language declaration. Lifecycle UI uses 搁置、纸篓、移至纸篓…、放回…、返回研究文档 and localized note counts, while `Trash/`, researcher titles, paths, exact Markdown, and Skill names remain verbatim. The catalog validator and six focused localization tests pass; packaged QA also covered Bootstrap, vault/Library/Attention labels, and native accessibility localization. | Researcher terminology review, long labels, and broader manual accessibility. |
| Beta app handoff | Copies immutable instructions before explicit app choice, persists one app-wide bookmark, opens without research arguments, and offers Copy Only, Choose Another, and Forget. Focused tests cover ordering, cancel/failure, replacement, forgetting, and persistence. | Packaged sandbox launch, Full Keyboard Access, VoiceOver, localization, and visual acceptance. |
| 1.0 Codex handoff | The target requires a new task at the exact root, locator-only composer, explicit submission, copy fallback, and no execution claim. | Availability/root checks, new-task adapter, composer, and Codex acceptance are unimplemented. Run with Codex remains a separate 2.0 decision. |
| Research Status | The role-neutral targeted editor preserves absent/declared/invalid states and unrelated bytes. New Analysis offers Declare Now/Not Yet; Not Yet writes nothing and blocks only Complete Review. Focused tests and repository verification pass; existing notes are not migrated. | Final visual and accessibility acceptance. |
| Time and Analysis metadata | Creation/modification times stay in app History; existing timestamp keys remain custom YAML. Debate Importance is an optional 0–10 whole number paired with exact Debate Scope and replaces Project Relevance only in typed defaults. | No migration or normalization of existing custom keys. |
| Long-source analysis | One source-level Analysis accumulates bounded units through Research Status. Source Analyzer is an independent copy-on-adoption Skill, not a Research Function; mutation remains separately authorized. | Dedicated long-source progress presentation. |
| Recommended Bibliography | Triptych-wide records survive scope/selection changes, support goals/purpose, retain prior results on refresh failure, accept zero results, discriminate conservatively, and persist without Markdown/Zotero mutation. Controller, CLI, and disposable UI automation cover transport. | Preparation still binds an Analysis rather than the Triptych; philosophical value and genuine spoken VoiceOver remain researcher-owned acceptance. |
| Protected Skill IDs | A colliding Triptych package stays visible, invalid, and recoverable while the bundled package remains authoritative. | Researcher must rename or delete the collision. |

## Retired reachable surfaces

- Proposal/Revision review sheets, Research Task/Session, Agent Assessment, and
  the former Agent Review surface.
- Active-note HTML/PDF export and Canvas.
- Zotero data-folder/SQLite access, additional-vault/All Notes presentation,
  and generated `_index.md`, `_agent-index.json`, or `_agent-context.json`.
- Obsolete Proposal, Research Session, workflow-bridge/readiness/lint,
  pre-release Review-store, and Add Dated Reference implementations.

Unsupported pre-release state is not read into the current interface. The
clean cutover never authorizes deleting or rewriting researcher Markdown,
unknown YAML, or unrecognized Triptych files.

## Current interface boundary

| Area | Current state | Still open |
| --- | --- | --- |
| Ownership and shell | One native Library–Document–Inspector split has scoped controllers and stable sessions. AppKit owns geometry; `WorkspaceWindowCoordinator` owns the exact lifecycle. `WindowModel` now delegates transition generation/flush ordering, presentation persistence, and Triptych resolution to three narrow coordinators without duplicating existing feature owners. Role-owned opaque backgrounds extend beneath the native titlebar, with content inside its safe area. The one inert-from-first-frame toolbar is divided by native tracking separators; each peripheral has one real toolbar item that presents Hide in its visible pane section and crosses into the Document section as Show when collapsed. Native Inspector transitions and the Library's 300 pt content minimum add no second geometry owner. | D-091 migration debt remains explicit: either approve the tracked-toolbar transfer as the target or define a public-AppKit, pointer-hittable pane-owned host. Fullscreen, automatic-collapse recovery below the expanded Library threshold, and the complete adaptation matrix also remain open. |
| Scenes, tabs, and Document | Codable routes use `windowID` as session identity; failed scene replacement retains the source. A window admits one tab per stable document target and selects an existing member on repeated open. All tab closes flush first. Leased/pinned sessions retain safety state; clean detached zero-lease sessions release editor/WebKit state immediately, with only bounded lightweight presentation retained. Debug launch uses a real repository-local bundle through LaunchServices. | Clean-account/restoration/Dock/New Window journeys, final tab visuals/replacement automation, and Editor adaptation acceptance. |
| Library, Search, and research | Filter, scoped Debate Importance ordering, Unclassified, Set Aside, and Trash are reachable. Search flushes first, rejects stale results, and has no vector/AI/chat path. The per-window Inspector restores Overview, Connections, or Functions; role-valid launchers show compact activity while Target/Materials stay in typed panels. Research Record follows focus and retains history. The former presentation registry and Document Research Strip are removed; focused automation covers restoration and pending-autosave preservation. | Complete three-mode visual/keyboard matrix, real localized assistive-technology acceptance, and broader Search ranking/usability review. |
| Library lifecycle destinations | D-088 keeps Set Aside and Trash on the Library plane: only heading and hierarchy content change; the retained tree becomes inert/inaccessible and surrounding geometry stays fixed. D-087 grid roles supply all spacing and targets. State tests cover loading, error, retry, switching, dismissal, stale completion, Attention exclusion, and retained context; disposable XCU covers navigation, geometry, Put Back, truncation, and deletion invalidation. | Human checks remain for widths, appearances, contrast/transparency/motion settings, Full Keyboard Access focus, and genuine English/zh-Hans VoiceOver. Automation is not visual or assistive-technology acceptance. |
| Visual system | D-087 leaves shell geometry to AppKit and gives semantic rhythm to `ScholiumGrid`; Read/Live/Source use explicit Web units. D-093 removes the document maximum measure while retaining 20/32/40 CSS px minimum divider separation, and replaces Document Styles with machine-local named Appearance configurations for Body, headings, and each Callout. Exactly one configuration is selected; save, rename, duplicate, delete, and additive Advanced CSS paths are implemented without a generated CSS preview. Under D-092, Accent `#A94C22` and Paper `#F8F0E2` remain the only color inputs; one resolver supplies 18 native/WebKit roles and a shared Library/Apparatus surface. Mapping, persistence, localization, renderer parity, and contrast checks pass. | Grid, Web metric, and Appearance values remain provisional pending researcher visual approval. The isolated QA bundle built, but this task did not obtain a reliable Settings screenshot because the local UI-control channel disconnected. Production-runtime acceptance and real contrast, transparency, motion, inactive-window, localization, IME, and assistive-technology coverage remain open. |
| Derived state and conflicts | Graph publication does not block activation/Search; the last good snapshot reports current/stale/failed state. Contained normalized paths preserve equivalent macOS paths. Conflict comparison binds exact editor/disk revisions, defaults to Compare, restores focus, and permits Reload only for the displayed disk revision. | Adjacent recovery-state and multiwindow acceptance within the complete UI gate. |
| Upgrade safety | The fail-closed distinct-build gate uses an isolated disposable byte-hostile Triptych and retains manifests/results. One run preserved path, size, SHA-256, permissions, and mtime for all 195 authoritative files. | Rerun for every release; this is not private-vault or installed-app evidence. |

## Remaining completion work

| Workstream | Implemented boundary | Required next evidence or work |
| --- | --- | --- |
| Editor semantics and components | `MarkdownSemanticDocument` is the sole shared projection. The locked dialect, retained Host, shared components, bridge v4, semantic scroll, guarded projection, exact-source reveal, ordered CSS layers, heading highlighting, and half-open Callout activation have automated coverage. Display mathematics is centered, italic, and automatically numbered at the physical right on both surfaces; code and tables retain shared protected styles. The superseded render authority, KaTeX 0.18.0 pin, and line-style imitations are absent. | Human side-by-side, mixed-script, and user-theme review; real keyboard, speech, text-service, CJK/RTL/IME, composition-recovery, and retained-memory journeys. Syntax/parser changes must extend shared fixtures first. |
| Complete interface gate | Automated responsive, conflict/recovery, localized, and focused editor journeys provide partial evidence. The runner now has explicit `smoke` and `complete` profiles; complete dynamically enumerates tests, builds once, then runs serially without rebuilding. Each app launch receives isolated `SCHOLIUM_HOME`, `CFFIXED_USER_HOME`, session identity, and disabled system restoration. The complete-class 2026-07-20 run remains 26 passed/37 failed; later focused passes do not convert it into a green suite. | Run the repaired complete profile, classify every remaining skip/failure, and finish adjacent ready/empty/loading/stale/error/conflict/recovery, fullscreen, automatic-collapse, multiwindow, clean-account, restored-session, and real-system-setting matrices. |
| Performance | Privacy-safe instrumentation, strict validation, exact WebKit attribution, fail-closed sampling, and RDF-1 v2 exist. Editor hot paths use a cached projection index, bounded inline updates, coalesced bridge v4 reports, nonpublished selection, and one-shot Read restore; deterministic workload regressions pass. One-sample diagnostics and incomplete 3/5-sample memory prefixes remain nonrelease evidence. | Measure the new path, complete the 51-sample memory scenario, add visible Editor latency actions, approve thresholds, freeze/tag/package the source, and run the 5-warm-up/30-sample G7 protocol on R1. |
| Search | Deterministic Title/Alias/Heading/Body ranking and separate direct graph relations are implemented and exercised on a 2,056-note collision fixture. | Broader ranking-usability evaluation and release-owner approval; vector, embedding, AI-ranking, and chat Search remain out of scope. |
| 1.0 Codex handoff | Copy-first Beta handoff is separate and reachable. | Implement supported-app/root validation, new-task adapter, locator-only composer, explicit submission/copy fallback, retry/cancel/Unicode/accessibility/reactivation, and unchanged-run recovery. Do not begin Run with Codex. |
| Distribution | Local source-first packaging, resource/signature validation, CLI execution, ZIP, and checksum smoke exist. | Clean exact tag, external install, Developer ID/notarization as applicable, published source/artifact provenance, and G9. |

Permanent deletion no longer advertises Checkpoint or Research Record recovery.
Disposable-filesystem tests cover coordinated deletion, checkpoint
invalidation, durable recovery notice, per-file inspection, and record-only
resolution.

## Verification evidence

This table retains representative evidence, not every run. Superseded detail
remains in Git history; temporary results are not durable. All research
fixtures were disposable, and Zotero testing made no live-library write.

| Evidence class | Latest recorded result | Interpretation |
| --- | --- | --- |
| Editor implementation | On 2026-07-21 the locked toolchain passed typechecking and 123 WebEditor tests. Twelve cross-runtime fixtures cover exact spans, malformed syntax, embeds, BOM/newlines, Unicode, CJK, and RTL. Workload tests keep index cardinality fixed and bound 1,000 same-paragraph selections to the old/new 2,000-UTF-16 neighborhoods without document/tree rebuild. Focused Swift/WKWebView journeys cover projection, activation, shared geometry, bridge v4 recovery, one-shot Read restore, retained state, semantic scroll, 100,000-character CJK editing, and nine adaptive-grid scenarios. XCU could not target the virtualized 100,000-character AX node, so that UI edit did not occur. | Automated correctness and computed-style evidence only; not human visual, assistive-technology/installed-IME, measured visible-performance, or release acceptance. |
| Repository verification | The 2026-07-22 `verify.sh` run passed 123 editor tests; 398 Core, 38 Contracts, 68 Application, and 256 App tests; reproducible bundle and CLI checks; isolation/dead-path checks; Research Function and Recommended Bibliography CLI executable lifecycles; optimized Release build; cold index rebuild; and RDF-1 v2 (800 notes, tree hash `bcef4ae9addf1dd01752cb5d21fb3309f7c487eb53b0b0b9e7df048f785997f9`). An earlier dirty-checkout arm64 packaging smoke passed resource, signing, and CLI checks. | Current-checkout headless verification and local smoke only; no clean tag, external install, human visual result, packaged performance, notarization, G7, or G9. |
| Product Skills | Verification passed five Workflow packages, protection/dependency/citation contracts, evolution/recovery, Dialogue/bootstrap, and Zotero. A disposable Settings journey covered guarded replacement and recovery. See [package architecture](../ScholiumCore/Resources/Skills/README.md) and [field trials](../ScholiumCore/Resources/Skills/evals/REAL_WORKFLOW_ASSESSMENT.md). | Structural/transport evidence only; synthetic evaluation proves no philosophical quality. Field-trial and manual accessibility acceptance remain open, so G10 and J-014–J-016 are incomplete. |
| Functions and CLI | Focused Contracts/controller/Application/installer/Settings tests and the executable lifecycle passed Dialogue contracts, routing, installation, strict parsing, function availability, promotion, conditional Revise, Awaiting Fidelity child/link/reuse, cancellation, bibliography, transports, and declared failure cases. | Executable transport, persistence, and orchestration; philosophical adequacy remains researcher-owned. |
| Search and derived state | SearchIndex passed the disposable 2,056-note collision fixture for deterministic field precedence, equal-rank path order, repeat stability, and incompatible-contract replacement; isolated UI verified visible match context. | Semantic and isolated UI evidence; broader ranking-usability evaluation remains open. |
| UI journeys | A 2026-07-22 complete-profile baseline recorded 69 journeys: 40 passed, 26 failed, and 3 skipped. Subsequent disposable focused runs passed exact Zotero source status, dirty-editor termination recovery, dirty Live Preview switching, 100,000-character CJK editing, external rename, exact Critique passage selection, and clean-account restoration. Eight Dialogue-dependent journeys are now explicitly deferred pending Dialogue redesign. A QA-only exact window-targeting hook compiles, but it and the remaining multiwindow/focus/checkpoint journeys were not rerun after foreground-window changes disrupted automation sequencing; further GUI runs were stopped. | Partial disposable automation only. The focused passes do not make the complete profile green. Dialogue redesign, final multiwindow/focus and checkpoint journeys, complete current acceptance, packaged performance, upgrade evidence, VoiceOver, installed-IME, and human system-interaction acceptance remain open. |
| Upgrade safety | `verify-qa-upgrade-safety.sh` passed distinct baseline `587b93a0a710bac273601c1fa86ad4db1578702c6cf34884f525c3fb8df8000c` and candidate `b7b3e109cb37e83e4dd3d6c76dac26f04abac1fed019bf85ef518a446480e551` builds; all 195 vault files retained path, size, SHA-256, permissions, and mtime, with only allowlisted manifest/identity changes. | Evidence for those QA builds only; rerun every release. Not installed-app or private-vault evidence. |
| Performance | On an unlocked M4 QA host, one-sample `scenario_only` launch, Search, cold Read, and warm Read latencies were 4,479, 440, 7,264, and 1,580 ms—each above the proposed limit. Stable five-process attribution was observed, but retained artifacts contain only 3- or 5-sample prefixes, not 51 samples. | Actionable diagnostics, not percentiles or G7. Latency improvement, memory convergence, approved thresholds, and the exact packaged Release run remain open. |
| Distribution | A 2026-07-20 local arm64 Release smoke passed metadata, licenses, Editor resources, inside-out app/helper signing, strict signature verification, CLI execution, ZIP, and checksum. Provenance recorded `source_clean=false`; there was no exact tag and signing was ad hoc. | Local preflight only; not clean-tagged G9, Developer ID/notarization, external-install, or release-asset evidence. |

`BETA_RELEASE.md` fixes the source-first `v0.1.0-beta.1` policy: exact
`GPL-3.0-or-later` source plus optional ad-hoc-signed app ZIP and checksum.
Bundle metadata and packaging align, but no clean tagged artifact, external
install, Developer ID/notarization result, or public release has passed.
