# Scholium Implementation Status

**Audited:** 2026-07-27
**Target authority:** [SCHOLIUM_SPEC.md](SCHOLIUM_SPEC.md)
**Scope:** current reachability, verification evidence, migration debt, and open
acceptance only. This ledger cannot redefine the target specification.

The nonauthoritative implementation sequence and fresh-task handoff contract
are maintained in
[RESEARCHER_GOVERNED_SKILLS_IMPLEMENTATION_ROADMAP.md](RESEARCHER_GOVERNED_SKILLS_IMPLEMENTATION_ROADMAP.md).

## Reachable target behavior

| Area | Current reachable boundary |
| --- | --- |
| Triptych and storage | Multiple registered Triptychs use independently chosen Analyses, Topics, and Works; each window belongs to one Triptych. Portable control state stays beside Works in `.scholium`; machine/derived state stays in Application Support. The atomic v2 registry does not import the retired single-workspace registry. |
| Application storage bootstrap | D-104 is reachable: production composition validates the real per-user Application Support root before constructing `WorkspaceStore` or `WorkspaceRuntime`, has no temporary or implicit read-only fallback, and disables workspace commands in Storage Unavailable. The nonmodal app-root failure page provides default Retry, selectable Details, and Quit. Debug/QA isolation requires an explicit root. |
| Exact source | `VaultDescriptorAccess` now holds a root descriptor per operation, walks parents with no-follow `openat`, opens leaves nonblocking/no-follow, and accepts only immediately `fstat`-verified regular files. Loads, fingerprints, precommit checks, readback, and recovery verification use this boundary. Existing-file commits hold the original FD, copy and verify metadata, preserve advancing content mtime, synchronize staging and parent, and use inode-guarded swap-back plus retained evidence on uncertainty. ACL and ordinary xattr/Finder-tag values remain exact; the system-managed quarantine attribute may normalize only its agent and forward-moving timestamp while flags and event identity remain fixed. Focused Core tests reject quarantine authority changes, and the disposable dirty-Live save/switch journey now commits without the former false `commitUncertain` alert. `FilePresence` treats only `ENOENT` as absence. `FrontmatterPatchPlanner` validates complete Yams semantics and requires a unique bounded plain key: scalar edits change only the value token, the role-aware Research Unit uses bounded member/array replacements, and missing keys append only at a proven block-mapping boundary. Ambiguous YAML returns a typed Source-directed refusal without byte changes. |
| Refresh and disposable projections | One `WorkspaceRefreshCoordinator` serializes prepare → Search transaction → snapshot publication; monotonic request IDs, merged request payloads, waiter-scoped cancellation, and a source-mutation gate prevent overlapping builders and file/identity migration races. Each pooled vault owns one rebuildable `VaultSourceCatalog` of exact documents, descriptor-observed metadata, source versions, semantics, and source-bound Search projections. Ordinary changes reread/reparse affected notes; event loss/root discontinuity requests full reconcile. Three independent catalog actors may prepare immutable generations concurrently inside the one cycle; Graph, Search, and publish remain ordered. Snapshot assembly reuses the same generation's fail-closed resolved identity and fingerprint-bound title projection. Graph still rebuilds completely, `WorkspaceSnapshot` remains intact, and Search v4 applies a transactionally persisted, generation-checked delta in one database per Triptych; stale generations remain rejected after reopen. UTF-16 projection cursors are linear, and privacy-safe counters cover enumeration/read/parse, graph, Search, snapshot/source size, Application publication, and MainActor delivery. |
| Lifecycle and deletion | Direct New Note creation, duplicate, move/rename, Set Aside, Trash, exact-path Put Back, and permanent deletion route through Application and `VaultRepository`. Library Add/File/keyboard create an empty untitled note at the current vault root; an ordinary folder context action creates in that folder. Creation claims the first unoccupied default path atomically, refreshes and selects it within one serialized document transition, and opens no sheet. Deletion removes the Note, current Critique, machine-local prewrite recovery and execution, stable identity, active Discussions, and affected checkpoints; finished portable records retain a tombstoned participant instead of being silently erased. A durable higher-level deletion journal restores pre-commit state or completes post-commit privacy cleanup; concurrent path recreation is preserved for explicit recovery. |
| Move and import | Confirmed moves preflight destination and incoming-link revisions and rewrite only identically resolved links. Stable identity/records follow confirmed moves or reconciled external renames; ambiguity requires confirmation. Unclassified imports remain editable and classify into any vault. Multi-file failure rolls back revision-safe exact bytes or produces a visible external-recovery record. |
| Settlement, Discussion, and Critique | Settle is a researcher-owned, fingerprint-bound, replaceable current judgment stored as one portable file per Note; it is not an application-authored event history. Before that portable state commits, the current Note's exact bytes are pinned in machine-local recovery; the same stable Note and fingerprint reuse one pin. Repeating Settle can update rationale/date and backfill a missing pin without duplicating identical bytes. Changed Since Settled is derived from the current Markdown revision during snapshot assembly rather than written into a legacy pending-state file. Review alone saves a selected-range Comment directly as one researcher turn containing only the stable Note, current fingerprint, and inclusive line range; Edit and Source have no Comment route. The composer closes only after write acknowledgement and retains its text on failure. Comment opens no sheet or agent handoff. The first Discuss on a Comment-only draft resolves the editable Discuss Method/Profile into the same Discussion and Local-v2 run; later handoffs reload those frozen machine-local instructions. Bound Discussion reopens directly, and no duplicate active-Discussion row appears in Actions. Finished Discussions remain Research Records and are not silently injected as Critique evidence. Written annotation remains authoritative Markdown or a semantic Callout. D-105 cleanly removes Human Review, Qualification, ResearcherComment, app-owned Annotation, pre-Function Dialogue, and their stores, decoders, migrations, recovery, Search, and UI paths. A Work retains one attributed current Critique under `Works/Critiques`; Action-backed Critique execution uses a recoverable Local-v2 handoff and idempotently reconciles actionable findings, while Accept/Reject/Rebut and researcher Complete Round remain separate scholarly decisions. |
| Research Action contract foundation | Contracts expose bounded Action IDs, a collision-free researcher-owned creation route, public execution kinds, Action-specific Target roles, the frozen default role matrix, a unified native parameter model, fail-closed schema-v2 snapshots, and a separate record identity that permits only schema version plus Action ID. Action Profile schema 1 adds bounded presentation, applicable roles, visibility/order, seven closed native module kinds, source and feedback requirements, readable-role declarations, direct-write role ceilings, two existing-note write operations, and exact property boundaries. A Triptych-portable, revision-checked Profile document stores those Profiles and their exact Skill-package binding; direct and explicit multi-Triptych copies remain independent. Application resolves role-valid default and researcher Actions, rechecks every preparation, and freezes exact Target, Method/resource revisions, Profile/document revisions, parameters, and concrete read/write authority without exposing the protected Function ID. Profile storage remains nonexecuting and a Profile cannot grant authority. Machine-local standing policy is now a separate bounded decision factor with exact Skill/Profile-envelope invalidation. Property-only custom mutation fails closed until its delta can be enforced. |
| Agent change request coordination | Schema-v1 requests bind one idempotent request ID to the exact Triptych, authenticated Local Execution v2 parent, parent and requested Skill/Profile revisions, stable Note identities, expected fingerprints, bounded existing-note operations, and attributed agent reason. Every new local Action run stores a short-lived coordination-key digest bound to the exact Triptych, parent run, and Action revision, while the plaintext key remains only in its live delivery packet. The App-wide AF_UNIX bridge retains its private ownership, same-UID, bounded-frame, timeout, and uncertain-outcome rules. One MainActor presentation coordinator now claims each pending request in at most one exact matching Workspace window, prefers that Triptych's key window, safely transfers an unresolved claim when a window closes, and lets `show_note_change_request` focus only the existing sheet. The native sheet derives current titles, roles, and revision state; resolves current Action and Skill display names; shows operations and Method/Profile revisions; exposes subset selection, Allow These Notes Once, Continue Without Changes, and Cancel the Run; keeps stale/expired state readable; and restores the prior responder after dismissal. Allow stays disabled if display identity is loading or unavailable, while raw package ID remains separately labeled; bounded identity retry and later exact replay/refresh can recover a transient failure. An authenticated bridge submit binds the request before an App-wide Triptych editor flush. Manual and automatic decisions then revalidate parent, Action, Skill, Profile, standing policy, roles, lifecycle, identity, operation, and fingerprints; the automatic path additionally requires matching repeated policy evaluation bound to the frozen package and Action/Profile role revisions, followed by one final complete request revalidation. Active Working Method, Action Profile, Skill content/maintenance, and policy writes share that decision gate, so an in-App mutation cannot interleave before the durable decision write. At request expiry the sheet removes decision controls immediately and performs bounded durable-state retries before retaining a contract-derived expired state. A currently qualifying standing policy records the exact allowed subset without a sheet. Every result remains non-authorizing coordination state and never widens the parent snapshot or grant. An allowed schema-v2 record now stores a versioned correlation plan for the exact approved subset; Application re-resolves live Action, Profile, Method, identity, and fingerprints before preparing one independent single-Target Local Execution v2 child per approved Note. Each child owns its reserved run ID, frozen snapshot, exact-Note continuation recovery checkpoint outside rolling automatic retention, activity grant, completion validation, final-revision Fidelity route, cancellation/conflict recovery, and parent/request/group lineage. The bridge returns live `child_preparations` only when every packet matches that durable plan; keys stay delivery-only. Analyze to Synthesize, Critique to Write, and activated Manuscript to Write use the same mechanism. Synthetic UI evidence cannot certify genuine VoiceOver, Full Keyboard Access, installed IME, localization quality, or researcher visual acceptance. |
| Analyze source access | `ResearchSourceReference` schema 1 retains only route, stable source identity, exact Zotero item/attachment keys where applicable, display filename, and fingerprint. A per-Triptych single-link mode-0600 Application Support file alone retains read-only bookmark and canonical path; descriptor-relative traversal rejects owned-path symlink, hard-link, nonregular, permission, and substitution attacks. Source hashing is bounded to the opened starting size and rejects concurrent change. The exact Zotero route refuses redirects, requires its original loopback response URL and absolute query-free file URL, and rechecks parent, attachment, selected path, source revision, and current nonempty Analysis key. Prepared and legacy Analyze runs fail closed on delivery and completion when exact source authority is absent. Permanent deletion preflights store health and removes its machine-local locator through the deletion recovery boundary. The production Analyze sheet now keeps a source-repair Action reachable while execution is disabled, accepts one researcher-selected local regular file through the native Open panel, re-resolves availability after binding, and shows only the safe filename. Packaged-sandbox reopen of a researcher-selected external source and an in-sheet Zotero attachment chooser remain unverified. |
| Research Actions and protected Functions | One per-window Inspector exposes Overview, Connect, and Actions; Document has no Research Strip. The production Inspector, Research menu, focused keyboard route, and common modular sheet now use public Action identity. New-Target checks clear and bind availability to the exact Target; same-Target checks retain visible rows in a disabled state, and late results cannot become actionable. Opening saves only the current Target editor, presents the already-visible Profile immediately, and loads declared Note/Source data inside the sheet; preparation still rejects any change to the presented execution kind, complete Profile semantic revision, or Profile-document revision without flushing unrelated Notes. Current Actions create no automatic whole-Triptych checkpoint; mediated writes preserve exact displaced bytes per Note, while manual checkpoints remain available. Both Action and retained Function preparations pass through the same resolver, embed the exact Action snapshot, and persist protected snapshot, instructions, grant digest, static Discuss transport contract, and completion evidence as independent Local Execution v2 files; a write report, grant completion, Function completion, and submission digest commit in one run-file replacement. Legacy stores remain reveal-only. Analysis Develop/Analyze resolves and rechecks one exact source before and after Method resolution; Topic Develop/Synthesize does not. The durable snapshot stores only the safe source reference, while the assembled packet may carry its absolute path as a transient locator. The retained coordinator continues to own completion keys, revision/write validation, containment, conflicts, and recovery; Function-named routes/controllers remain compiled but have no production Inspector or menu entry pending Session 22 deletion. |
| Action write scope and Fidelity | Current Analyze, Synthesize, and Write preparation fixes the exact current Note as the sole write Target. The common Action sheet presents the Target, current fingerprint prefix, declared candidate authority, and revalidation/checkpoint/conflict/recovery boundary before its Profile modules. Contract validation rejects legacy multi-target write payloads before checkpoint or grant creation while preserving their decode/round-trip shape. The retained activity store and multi-target Fidelity data structures remain internal compatibility until independently authorized child phases replace their old public use. |
| Recovery and chronology | Researcher-created self-contained checkpoints support comparison, selective/full restore, and Finder access; retained legacy automatic checkpoints remain recoverable but are not created by current Actions. Core keeps a non-delivery SQLite/immutable-object prewrite ledger with v1 preservation, corruption quarantine/rebuild, remap, temporary retention, deletion tombstones, and settled-version pins. Durable pin manifests own exact-byte identity and per-Note monotonic order; a common advisory lock and unique index coordinate allocation, while one-transaction UPSERT replaces a derived SQLite row when any manifest field differs. Exact-valid manifests protect bytes even when projection is ambiguous; that condition blocks new recovery writes and all automatic cleanup. Portable replacement distinguishes proved pre-rename failure from commit uncertainty, so only the former can roll back a new pin. Machine-local per-Triptych policy retains 10, 30, 50, or no automatic limit per stable Note, defaults to 30, and requires an exact-ID preview before a lower limit removes older pins. The approved set remains in a bounded 8 MiB journal until cleanup completes. Terminal validated nonconversational Action runs create one whitelisted portable record under `.scholium/research-records/v1/records/`; it records the Action rather than its protected Function and recursively rejects undeclared fields and path-shaped nested values while excluding prompts, raw keys, bookmarks, absolute paths, diffs, token counts, transport logs, and window state. Portable active Discussion now uses one file under `active/`, and Finish creates exactly one neutral finished record with interruption reconciliation. Corrupt peer files are isolated. The independent list/detail window is reachable; manual Record Trash, settled-version restore UI, and disposable diff remain later work. |
| Application and CLI | GUI and CLI share Application capabilities and trust rules. Each CLI invocation uses one snapshot runtime; existing-note mutations require current SHA-256. App/CLI import only Contracts and Application, Core remains internal, one live app runtime has one accepted event subscription, and per-window controllers own independent presentation state. |
| Connections, Search, and Attention | One workspace `GraphSnapshot` owns Vector-Link v1 resolution; neutral/transitive paths never become evidence and legacy syntax remains exact with diagnostics. Search contract v4 uses one normal-content SQLite FTS5 corpus per Triptych for GUI/CLI Vault and Triptych retrieval, while This Note searches the unsaved editor snapshot by occurrence. The removed `status` and `review` fields have no projection, index column, filters, or ordering; `status:` and `review:` fail explicitly without affecting ordinary lexical search. Attention is limited to specified structural, currency, connection, reliance, metadata, and identity conditions; dismissals are Triptych-local. |
| Editor | CodeMirror-backed Edit/Source and sanitized Review share one checked semantic projection and the same callout, table, footnote, math, preview, font, and presentation contracts. Generated callout roles are accessibility-only; supplied titles inherit role styling. One retained Host keeps Review and one CodeMirror state mounted; stable internal identifiers remain `read`, `livePreview`, and `source`. Bridge v5 preserves exact source, identity, recovery, focus, scroll, and nonmutating UTF-16 Search-result reveal while coalescing selection reports. Edit's restrained selection toolbar exposes only common Markdown formatting. Review owns a separate direct line-only Comment bar; Return saves, Shift-Return inserts a line, Escape cancels, and Edit/Source have no Comment surface. Review alone owns footnote hover preview, navigation, and return; Edit's rendered footnotes are passive CodeMirror projections and Source remains exact text. Automated TypeScript, Swift, real-WKWebView, and isolated QA cover implemented semantics; human visual, assistive-technology, IME, retained-memory, and sustained-performance acceptance remain open. |
| Properties and Zotero | Contracts now separate canonical vocabulary/ownership, default About profiles, and empty creation requirements. Analysis uses Completion/Limitations and retains hidden title plus protected `zotero_item_key`; Topic/Work use Scope/Limitations and H1→filename identity, with Work labelled Research Scope. `status`, Work `deadline`, required markers, About Customize, and default disclosure state are removed. Inspector shows no Zotero projection. An eligible Analysis Research Function reads the exact item once, attaches the protected Zotero integration Skill, and stores labelled bibliographic metadata or a nonblocking warning in its immutable run snapshot; resume reuses it and a new run rereads without cross-task cache. Bibliographic metadata remains nonblocking, but a Zotero attachment selected as Analyze's required source is independently exact and blocking. |
| Product Skills | Catalog schema 4 separates protected mechanism from seven Action-addressed bundled Method references. A new Triptych now installs six independent editable working packages before its manifest, writes explicit Action-keyed binding v2 states, and records Manuscript as disabled. Analyze and Synthesize resolve independently over retained Develop; every current Action's primary Method uses only v2 and fails closed for missing, malformed, disabled, invalid, incompatible, dependency-broken, or Method-dependent state. Bundled Methods are read-only installation/restore references, never runtime fallback. Protected System mechanism remains independently seeded, exact Method resources are coherently fingerprinted, and direct edit/restore publish displaced packages through the existing machine-local maintenance snapshot lifecycle. Verified cross-volume copy retains and reports the hidden portable inode instead of deleting possible late writes. Source Analyzer, APA 7 verification, Philosophical Practices, and Prose Control remain opt-in Researcher templates. |
| Researcher Skill installation | An app-wide staged installer accepts one researcher-selected local directory, retains exact UTF-8 bytes only in an expiring Core preparation, and exposes a nonexecuting inventory that contains no source path or file contents while showing purpose, declared metadata, proposed Action placement, and the explicit requirement for a later Action Profile. Descriptor-relative traversal accepts only `SKILL.md` plus one-level regular `references`, `templates`, and `evals`; bounded enumeration rejects the 129th file without first collecting an unbounded listing, and input also fails closed for network URLs, archives/non-directories, symlinks, hard links, executable or scripted files, unsupported/nested resources, malformed metadata, files over 1 MiB, and packages over 8 MiB. Application resolves explicitly selected Triptychs; Core preflights all destinations, rejects dangling current bindings or the retained citation/bibliography bindings that can still execute, uses no-replace publication for each independent copy, and repeats bounded validation after publication. Ignored legacy Function/Skill/Practice fields cannot authorize or block new installation. A later-destination failure moves proved task-owned directories into hidden nonexecuting recovery quarantine without deleting possible late writes; moved/replaced or otherwise unprovable identities return recovery-required. The installer writes no Profile, binding, permission approval, or execution state, so an unbound package starts disabled. |
| Research Guidance | Production Settings now presents one restrained categorized list-detail surface for Methods, Researcher Skills, Permissions, Sources and Integrations, and Recovery and Technical. It exposes explicit Working Method installation for established Triptychs; revision-checked direct edit, disable, compatible replacement, bundled comparison and restore; hidden Manuscript activation and direct edit; staged local-directory installation; local Skill creation, structural validation and recoverable guarded deletion; and Action Profile creation, confirmed deletion, global reordering, all seven declarative modules, capability declaration, nonexecuting preview, deliberate Show in Actions, and explicit independent copying to selected Triptychs. Recovery and Technical exposes the per-Note settled-version policy with a destructive confirmation only when lowering the limit enumerates older versions. Skill and Action Profile drafts are isolated by Triptych, package, and Action identity, survive category and Settings-tab navigation until saved or discarded, and cannot cross a Triptych switch; cancellation and identity checks also prevent stale Methods, Skills, Recovery, or Permissions reloads from repopulating a new Triptych. One Skill cannot silently claim an Action ID already configured by another. Existing Function-keyed Settings and `research-skill-bindings.json` remain readable legacy state but are not projected into this surface and cannot enter Action resolution. Citation Fidelity and Recommended Bibliography retain separate v1-compatible capability bindings. Permissions now edits the machine-local Triptych default and exact-envelope per-Skill overrides, exposes invalidation without broad fallback, and states the boundary around Scholium-mediated operations. Sources reuses current CLI/Zotero controls; recovery can reveal Skill and unchanged legacy-data locations but has no staged-install quarantine cleanup. The prior Settings implementation remains compiled but is unreachable migration code pending Session 22 deletion. |
| Research workflow interface proofs | A synthetic Debug-only PreviewCatalog now covers the frozen Analysis/Topic/Work Action matrix, one modular Skill-run sheet, staged local-directory installer, responsibility-based Research Guidance categories, Agent-requested additional Notes, the fixed secondary Research Record utility layout, and empty/loading/error/conflict/permission-invalid states. Named previews expose dark, increased-contrast, reduced-transparency, reduced-motion, and 200% legibility review entries. The catalog is suppressed by default and its QA command is available only to the Debug `com.scholium.qa` bundle with an explicit launch argument. It owns no Triptych, Skill, permission, execution, or record authority and is not a production surface. |
| Discuss and MCP | Line Comment, whole-note Discuss, and optional focal Notes now use one `PortableResearchDiscussion`; older exact-passage turns remain readable. A new Comment stores only its Note fingerprint and inclusive line range, never starts an agent run, and is collected when Discuss opens the active exchange. New runs keep only their static response transport contract in Local Execution v2; attributed turns live in the portable active exchange. Close has no storage effect. Agent reply evidence is required before a Discuss run can complete, while researcher Finish is a separate neutral transition into one portable Research Record and creates no legacy Discussed event. External Zotero MCP remains separate from the built-in reader; only explicit `--probe` performs its read-only initialization lifecycle. |

## Adopted target migration still outstanding

D-106 was adopted as target authority on 2026-07-24. Adoption alone did not
make the complete migration reachable; the table distinguishes the Action and
Method foundations now implemented from the remaining cutovers.

| Target migration | Current divergence | First implementation gate |
| --- | --- | --- |
| Research Actions | Versioned public Action identity, execution kind, role matrix, schema-v2 run snapshot, Action-only record identity, safe Source Reference schema 1, Action-keyed Method binding v2, disabled-first staged installation, general resolution, reproducible execution entry, Local Execution v2, terminal portable record writing, machine-local standing policy, typed Agent change requests, the local request bridge, native request sheet, and independently permission-bound continuation children are implemented. The production Inspector and Research menu now expose the exact Analysis, Topic, and Work matrices plus one ordered Researcher Skills group; they contain no Research Activity HUD, generic Research Record row, Work with Agent wrapper, or public Function name. One common Profile-generated sheet renders the closed native modules and the app-owned run boundary, including a reachable local-source repair for Analyze. Copy Only and Copy and Open remain fixed in its footer; either performs preparation internally, while a prepared run keeps the same actions available for retry. Availability and preparation re-resolve against the current Target and reject stale results. A load failure discards the launcher Profile, and a late result from noncooperative preparation is reclaimed through typed cancellation instead of becoming an invisible run. New Action preparation never inherits the retained Function-era implicit shared-Fidelity expansion; retained Function callers keep that compatibility path explicitly and record every expanded peer in their read envelope. The portable record serializes `ResearchActionRecordIdentity` and never a Function ID. All new Action and retained Function entry-point runs use Local Execution v2; legacy activity and Dialogue files remain untouched and unprojected. | Function-era CLI syntax and compiled retained presentation source remain migration debt until scheduled cutover/deletion. |
| Skill ownership and Settings | New Triptychs install independent editable Working copies for the six default Actions; binding v2 explicitly distinguishes installed default, Researcher Skill, and disabled, with Manuscript disabled until deliberately activated. Bundled references remain immutable. The categorized production Research Guidance surface reaches direct edit including activated Manuscript, disable, compatible replacement, restore, explicit default installation, staged local-directory installation, local Skill creation and recoverable deletion, Profile editing and global reordering, all seven bounded modules, declared read/write ceilings, disposable interface preview, deliberate visibility, explicit independent copying to selected Triptychs, and machine-local Triptych/per-Skill standing policy. Profiles use a revision-checked portable file; a Skill used by a Working Method, Profile, or retained executable v1 capability cannot be renamed or deleted. The general resolver consumes only v2 Working Method or exact Profile bindings and never reconnects legacy v1 selection. Existing Triptychs without v2 are not mutated automatically, and malformed v2/Profile/permission state fails closed. Cross-volume and staged-install recovery cleanup, malformed-v2 quarantine/reset, and Session 22 source deletion remain open. | Delete unreachable legacy Settings source only during the scheduled clean cutover. |
| Discussion and Research Record | One portable Discussion now carries passage anchors, whole-note exchange, optional focal Notes, attributed researcher turns, agent replies, and feedback. Active drafts stay in Actions; reliable anchor drift is reattached and ambiguity is marked. Finish atomically converges to one finished portable record without approval/rejection semantics. Permanent deletion purges active drafts and tombstones finished participants. The independent nonrestored utility window now indexes every finished portable Discussion and nonconversational Action in memory, opens scoped to This Note when available, and supports removable Note plus date, Skill, Action, participant, and Unicode text filters. Its fixed 760 × 680 list/detail presentation retains attribution, separates reported from confirmed evidence, exposes revisions and tombstones, deep-links live Notes by stable identity, and mutates only the portable pin field. | Session 20 adds manual Record Trash and disposable diff. |
| Permission and continuation | Profiles declare only a bounded readable-role and candidate-write ceiling and grant nothing by themselves. On initial Action preparation, the resolver intersects that declaration with exact selected inputs and the current Target to freeze one short-lived, nonreusable authority envelope; the coordinator still rechecks live identities and revisions. Machine-local Ask Me Every Time, Ask Me Only for Works, and Triptych-wide policies decide only whether a later validated request may be recorded without another decision. Per-Skill overrides bind to the Application-derived complete Skill/Profile envelope and become Ask Me Every Time after any digest change rather than falling through to a broad default. Initial explicit Action selection is not prompted twice. A prepared parent can receive one authenticated, idempotent typed request through the local bridge; standing policy or the exact native sheet records its non-authorizing decision without changing the parent snapshot or grant. An allowed subset reserves independent child identities, and every child repeats current-state resolution before receiving its own checkpoint, grant, completion and Fidelity boundary; lineage records correlation but never grants authority. | Continue to preserve the frozen-parent and delivery-only-key boundaries through later Research Record and Resynthesize work. |
| Interface | Production Actions now begins with role-valid full-row defaults, lets Discuss itself reopen an active exchange, follows with one Researcher Skills group, and retains Settle as a quiet current-state operation. The Research Activity HUD, separate active-Discussion row, Work with Agent wrapper, and duplicate Research Record row are absent; Connect remains its own Inspector mode and Research Record remains independent. The shared sheet appears from the already-visible Profile, loads only declared Note/Source modules in place, and uses restrained text, structural rules, bounded searchable selection, native controls, and no chat bubbles or middle-dot separators. Its footer exposes the two handoff outcomes directly rather than requiring a visible Prepare step or scrolling to a handoff menu. Research Record is a fixed 760 × 680 temporary utility window with one quiet list/detail layout and no Workspace-style Sidebar control or alternate wide/narrow mode; it presents restrained attributed prose, explicit pinning, stable accessibility labels, and no verdict controls. The researcher-facing modes are Review, Edit, and Source while their stable internal identities remain unchanged. Review's transient Comment bar and Edit's formatting bar are separate; neither exposes handoff controls. | Researcher visual acceptance, genuine spoken VoiceOver, physical Full Keyboard Access, installed IME, and the complete production appearance/adaptation matrix remain open. |
| Clean cutover | The superseded shipped Development, Revision, Discuss Response packages and the `Scholium Workflow Skills` resource root are deleted. Primary Method selection has cut over from Function binding v1 to Working Method binding v2. New Action and Settle writes no longer touch legacy activity, Dialogue, binding, or grant files. Those files are not migrated, deleted, rewritten, or projected as authority; byte/digest/mode/mtime canaries cover the new stores, and **Reveal Legacy Data** opens the exact machine-local Triptych directory. Temporary citation/bibliography fields, retained Function-era UI, stores, and records remain until their scheduled replacements. | Delete each remaining superseded repository path in the same step that makes its replacement reachable. Never migrate, delete, or rewrite researcher Markdown, unknown YAML, unrecognized Triptych files, or unsupported legacy data. |

The Product Skill rewrite may inspect the researcher's existing local
philosophical Skill library as methodological evidence, but it must re-author
each package for Scholium's Action, source-fidelity, authority, feedback, and
evaluation contracts. It must not copy package identity, prose, permissions,
or workflow assumptions wholesale.

The final cutover cannot close until a line-by-line omission audit compares the
implemented product and D-106 against the originating “A Place Where Thoughts
Grow Up” proposal and records every intentional deferral. The canonical
specification controls if exploratory prose and an adopted rule differ.

External research informed the implementation method, not Scholium's product
meaning: Apple's current [Settings](https://developer.apple.com/design/human-interface-guidelines/settings)
guidance supports stable related panes, few long-lived settings, and keeping
task-specific choices in context; its [Lists and
Tables](https://developer.apple.com/design/human-interface-guidelines/lists-and-tables)
guidance supports textual list/detail presentation. VS Code's official
[Contribution Points](https://code.visualstudio.com/api/references/contribution-points)
show a mature declarative separation among contribution metadata, placement,
context, and host execution. Zotero's official
[Settings](https://www.zotero.org/support/preferences) documentation provides a
research-software example of responsibility-based panes. Obsidian's official
[plugin guidelines](https://docs.obsidian.md/Plugins/Releasing/Plugin+guidelines)
reinforce host-owned APIs, disclosed external access, and no hard-coded host
styling. These sources neither authorize executable Scholium plugins nor prove
the proposed interface; static proofs, production integration, and researcher
acceptance remain separate gates.

## Adopted boundaries with remaining acceptance

| Boundary | Implemented evidence | Still open |
| --- | --- | --- |
| Localization | English default; complete zh-Hans string catalogs, compiler-synchronized formats, coverage/placeholder/state/compilation validation, and outer-bundle language declaration. Lifecycle UI uses 搁置、纸篓、移至纸篓…、放回…、返回研究文档 and localized note counts, while `Trash/`, researcher titles, paths, exact Markdown, and Skill names remain verbatim. The catalog validator and six focused localization tests pass; packaged QA also covered Bootstrap, vault/Library/Attention labels, and native accessibility localization. | Researcher terminology review, long labels, and broader manual accessibility. |
| Beta app handoff | Copies immutable instructions before explicit app choice, persists one app-wide bookmark, opens without research arguments, and offers Copy Only, Choose Another, and Forget. Focused tests cover ordering, cancel/failure, replacement, forgetting, and persistence. | Packaged sandbox launch, Full Keyboard Access, VoiceOver, localization, and visual acceptance. |
| 1.0 Codex handoff | The target requires a new task at the exact root, locator-only composer, explicit submission, copy fallback, and no execution claim. | Availability/root checks, new-task adapter, composer, and Codex acceptance are unimplemented. Run with Codex remains a separate 2.0 decision. |
| Properties and Research Unit | The role-aware targeted editor preserves absent/declared/invalid states and unrelated bytes. Analysis accepts binary or represented-ratio Completion and/or Limitations; Topic/Work accept Scope and/or Limitations. All fields are optional at creation, deleting one member retains the others, and focused contract/source-fidelity tests pass. The native comparison catalog includes mixed/all-serif, 320pt/narrow, light/dark, contrast, mixed-script, long-text, empty-Connect, direct-action, and unavailable-Fidelity cases. | Researcher catalog review must approve or revise the provisional typography and spacing before those values are treated as visually settled. Complete pointer/keyboard/focus/VoiceOver, installed-IME, and human visual acceptance remain open. |
| Time and Analysis metadata | Creation/modification times stay in app History; existing timestamp keys remain custom YAML. Debate Importance is an optional 0–10 whole number paired with exact Debate Scope and replaces Project Relevance only in typed defaults. | No migration or normalization of existing custom keys. |
| Long-source analysis | One source-level Analysis may record represented Completion such as `6/11`; this is a quiet reminder rather than a chapter ledger or adequacy claim. Source Analyzer remains an independent copy-on-adoption Skill, not a Research Function; mutation remains separately authorized. | Final Completion control and reminder presentation require human review. |
| Recommended Bibliography | Triptych-wide records survive scope/selection changes, support goals/purpose, retain prior results on refresh failure, accept zero results, discriminate conservatively, and persist without Markdown/Zotero mutation. Controller, CLI, and disposable UI automation cover transport. | Preparation still binds an Analysis rather than the Triptych; philosophical value and genuine spoken VoiceOver remain researcher-owned acceptance. |
| Protected Skill IDs | A colliding Triptych package stays visible, invalid, and recoverable while the bundled package remains authoritative. | Researcher must rename or delete the collision. |
| Architecture stability cutover | Focused automated suites pass descriptor and special-file rejection, inode/path races, metadata preservation, three-state deletion, fail-closed YAML, single-flight refresh, stale Search generation, shared-vault/per-Triptych-index isolation, incremental/full-rebuild equivalence, multiwindow identity migration, noncooperative lifecycle completion, storage failure/retry, research-data/Researcher-Skill injection boundaries, and the D-105 record-model cutover. The exact RDF-1 comparison now records 800-note cold builder wall time of 914.34 ms and add/edit/rename/delete at 162.29–172.27 ms after linear projection, same-generation identity reuse, and bounded three-vault preparation. Complete repository verification is green. The retained 2026-07-23 Complete UI profile predates D-105 and is historical evidence only. | A fresh disposable Complete UI profile must establish the next baseline; G7 threshold approval/packaged sampling, G9 release provenance, G10 field trials, and genuine accessibility/IME/visual acceptance remain separate evidence gates. |

## Retired reachable surfaces

- Proposal/Revision review sheets, Research Task/Session, Agent Assessment, and
  the former Agent Review surface.
- Human Review, Qualification, ResearcherComment, app-owned Annotation, and
  pre-Function Dialogue archives and compatibility paths.
- Active-note HTML/PDF export and Canvas.
- Zotero data-folder/SQLite access, additional-vault/All Notes presentation,
  and generated `_index.md`, `_agent-index.json`, or `_agent-context.json`.
- Obsolete Proposal, Research Session, workflow-bridge/readiness/lint,
  pre-release Review-store, and Add Dated Reference implementations.

Unsupported pre-release state is not read into the current interface. The
clean cutover never authorizes deleting or rewriting researcher Markdown,
unknown YAML, or unrecognized Triptych files.

## Current interface boundary

| Area | Current state | Still open |
| --- | --- | --- |
| Ownership and shell | One native Library–Document–Inspector split has scoped controllers and stable sessions. AppKit owns geometry; `WorkspaceWindowCoordinator` owns the exact lifecycle. `WindowModel` now delegates transition generation/flush ordering, presentation persistence, and Triptych resolution to three narrow coordinators without duplicating existing feature owners. Role-owned opaque backgrounds extend beneath the native titlebar, with content inside its safe area. The one inert-from-first-frame toolbar is divided by native tracking separators; each peripheral has one real toolbar item that presents Hide in its visible pane section and crosses into the Document section as Show when collapsed. Native Inspector transitions and the Library's 300 pt content minimum add no second geometry owner. | D-091 migration debt remains explicit: either approve the tracked-toolbar transfer as the target or define a public-AppKit, pointer-hittable pane-owned host. Fullscreen, automatic-collapse recovery below the expanded Library threshold, and the complete adaptation matrix also remain open. |
| Scenes, tabs, and Document | Codable routes use `windowID` as session identity; failed scene replacement retains the source. A window admits one tab per stable document target and selects an existing member on repeated open. All tab closes flush first. Leased/pinned sessions retain safety state; clean detached zero-lease sessions release editor/WebKit state immediately, with only bounded lightweight presentation retained. Debug launch uses a real repository-local bundle through LaunchServices. | Clean-account/restoration/Dock/New Window journeys, final tab visuals/replacement automation, and Editor adaptation acceptance. |
| Library, Search, and research | Filter, scoped Debate Importance ordering, Unclassified, Set Aside, and Trash are reachable. Search exposes This Note, This Vault, and Triptych only; current v4 parsing rejects `status:` explicitly. This Note reads the current in-memory editor revision without saving; all scopes cancel superseded requests and reject stale request, generation, fingerprint, session, or revision results. Related remains a separately loaded direct-connection region and has no vector/AI/chat path. The per-window Inspector restores Overview, Connect, or Actions. Overview omits empty About fields and all Zotero presentation; Connect preserves counted empty groups without filler; Actions presents the public role-valid defaults, grouped Researcher Skills, and Settle without chronology, a duplicate record launcher, or a separate active-Discussion row. Discuss itself opens an existing exchange. Research Record follows focus and remains separate. | Native catalog comparison and researcher typography approval; complete 320pt/narrow, mixed-script/RTL, contrast/motion, physical Full Keyboard Access and genuine spoken VoiceOver journeys; current Search timing and broader ranking review. |
| Library creation and context actions | Library Add creates at the current vault root without a secondary window; File and Shift-Command-N use the same focused-window path. Library now projects empty real directories. Ordinary folder menus provide direct New Note/New Folder, Rename Folder, Move Folder, conditional Expand/Collapse All, Copy Relative Path, Reveal in Finder, and confirmed Move Folder and Notes to Trash, with equivalent accessibility actions. Folder creation atomically advances default names. Empty-folder changes enter the serialized refresh owner and reread/reparse no Markdown; the current correctness-first builder may still reassemble the in-memory graph while Search detects an unchanged manifest. Folder moves containing notes preflight every descendant Markdown revision, rename the directory once, preserve non-Markdown bytes, rewrite exact resolved incoming links, batch-rebind stable note IDs, and reuse durable rollback/recovery. Note menus retain their open/lifecycle/Finder actions and Copy Relative Path. Ambiguous legacy projections and managed Critiques fail closed. Focused Contracts/Core/Application/App regression tests cover path validation, empty-folder projection, external inventory races, stable identity, links, and non-Markdown contents. A disposable XCU journey passed the complete folder path: all menu groups, direct sheet-free New Folder, rename, confirmed Trash, and subsequent direct New Note. | Measure whether empty-folder graph assembly is material after the catalog cutover. Full Keyboard Access, spoken VoiceOver, localization review, File-menu/shortcut coverage, case-only/normalization-sensitive volume coverage, injected folder-rollback failures, and final human interaction/visual acceptance remain open. |
| Library lifecycle destinations | D-088 keeps Set Aside and Trash on the Library plane: only heading and hierarchy content change; the retained tree becomes inert/inaccessible and surrounding geometry stays fixed. D-087 grid roles supply all spacing and targets. State tests cover loading, error, retry, switching, dismissal, stale completion, Attention exclusion, and retained context; disposable XCU covers navigation, geometry, Put Back, truncation, and deletion invalidation. | Human checks remain for widths, appearances, contrast/transparency/motion settings, Full Keyboard Access focus, and genuine English/zh-Hans VoiceOver. Automation is not visual or assistive-technology acceptance. |
| Visual system | D-087 leaves shell geometry to AppKit and gives semantic rhythm to `ScholiumGrid`; Read/Live/Source use explicit Web units. D-093 replaces Document Styles with machine-local named Appearance configurations for Body, headings, and each Callout. D-100 adds the one shared **Line width** control: **72ch** by default, normalized to **48–96ch**, applied to Read/Live/Source with 20/32/40 CSS px minimum divider separation and without changing Source typography. Exactly one configuration is selected; save, rename, duplicate, delete, backward-compatible manifest decoding, and additive Advanced CSS paths are implemented without a generated CSS preview. Runtime presentation CSS updates the retained Read WebView and CodeMirror editor, requests remeasurement, and preserves document bytes, selection, undo, focus, and the semantic scroll anchor. Under D-092, Accent `#A94C22` and Paper `#F8F0E2` remain the only color inputs; one resolver supplies 18 native/WebKit roles and a shared Library/Apparatus surface. Mapping, persistence, localization, renderer parity, responsive overflow, and contrast checks pass. A disposable pointer-and-keyboard journey also covered the default window, 1380/1080/900/720 widths, Inspector visibility, all three modes, 72→73 keyboard adjustment, save persistence, and retained-window screenshots. D-097 adopts one researcher-approved parchment-and-ink pointing-hand application icon, and every assembly path references the same repository-owned `.icns`. | The 72ch default and Appearance rhythm remain pending researcher visual approval; screenshot inspection is bounded automated evidence, not that approval. Genuine spoken VoiceOver, physical Full Keyboard Access, installed IME, inactive-window, Reduce Transparency/Motion, and production-runtime acceptance remain open. The icon passed file-level representation inspection, but Finder, Dock, small-size, appearance, and packaged-Release visual acceptance remain open. |
| Derived state and conflicts | Graph publication does not block activation/Search; the last good snapshot reports current/stale/failed state. Contained normalized paths preserve equivalent macOS paths. Conflict comparison binds exact editor/disk revisions, defaults to Compare, restores focus, and permits Reload only for the displayed disk revision. | Adjacent recovery-state and multiwindow acceptance within the complete UI gate. |
| Upgrade safety | The fail-closed distinct-build gate uses an isolated disposable byte-hostile Triptych and retains manifests/results. One run preserved path, size, SHA-256, permissions, and mtime for all 195 authoritative files. | Rerun for every release; this is not private-vault or installed-app evidence. |

## Remaining completion work

| Workstream | Implemented boundary | Required next evidence or work |
| --- | --- | --- |
| Editor semantics and components | `MarkdownSemanticDocument` is the sole shared projection. The locked dialect, retained Host, shared components, bridge v5, semantic scroll, guarded projection, exact-source reveal, ordered CSS layers, heading highlighting, and half-open Callout activation have automated coverage. Display mathematics is centered, italic, and automatically numbered at the physical right on both surfaces; code and tables retain shared protected styles. The superseded render authority, KaTeX 0.18.0 pin, and line-style imitations are absent. | Human side-by-side, mixed-script, and user-theme review; real keyboard, speech, text-service, CJK/RTL/IME, composition-recovery, and retained-memory journeys. Syntax/parser changes must extend shared fixtures first. |
| Complete interface gate | Automated responsive, conflict/recovery, localized, and focused editor journeys provide partial evidence. The runner has explicit `smoke` and `complete` profiles; complete dynamically enumerates tests, builds once, then runs serially without rebuilding. Each app launch receives isolated `SCHOLIUM_HOME`, `CFFIXED_USER_HOME`, session identity, and disabled system restoration. The fresh 2026-07-23 Complete run finished without automation or infrastructure timeout: 72 enabled journeys produced 49 passed, 22 failed, and 1 skipped; the manifest separately retained 3 disabled specialized journeys. | Repair the 22 application failures, then rerun the same manifest and finish adjacent ready/empty/loading/stale/error/conflict/recovery, fullscreen, automatic-collapse, multiwindow, clean-account, restored-session, and real-system-setting matrices. |
| Performance | Privacy-safe instrumentation, strict validation, exact WebKit attribution, fail-closed sampling, and RDF-1 v2 exist. Editor hot paths use a cached projection index, bounded inline updates, coalesced bridge v5 reports, nonpublished selection, and one-shot Read restore. The cold-start cut removes repeated UTF-16 scans, per-Work identity reads, and serial three-vault preparation; deterministic 100,000-CJK, RDF-1, refresh-concurrency, Search, and visible-library diagnostics pass. Three Debug launch samples and incomplete 3/5-sample memory prefixes remain nonrelease evidence. | Complete the 51-sample memory scenario, add visible Editor latency actions, approve thresholds, freeze/tag/package the source, and run the 5-warm-up/30-sample G7 protocol on R1. |
| Search | Contract v4 retains one Triptych corpus and the v3 lexical behavior while deleting semantic `status` projection and query support. Finite syntax, visible-semantic projection, CJK verification, exact identity, rank reasons, This Note occurrences, CLI parity, atomic generations, and direct Related remain implemented. | Complete focused v4 regression, current 30-sample GUI first-paint protocol, disposable UI matrix, and human pointer/VoiceOver/Voice Control/Dictation/CJK IME/visual/ranking acceptance. |
| 1.0 Codex handoff | Copy-first Beta handoff is separate and reachable. | Implement supported-app/root validation, new-task adapter, locator-only composer, explicit submission/copy fallback, retry/cancel/Unicode/accessibility/reactivation, and unchanged-run recovery. Do not begin Run with Codex. |
| Distribution | Local source-first packaging, resource/signature validation, CLI execution, ZIP, and checksum smoke exist. Debug, QA, and release assembly all copy the D-097 canonical application icon named by the bundle plist. | Clean exact tag, external install, canonical-icon Finder/Dock inspection, published source/artifact provenance, and G9. Developer ID and notarization remain outside the current source-first Beta gate unless a later distribution channel adopts them. |

Permanent deletion no longer advertises Checkpoint or Research Record recovery.
Disposable-filesystem tests cover coordinated deletion, checkpoint
invalidation, durable recovery notice, per-file inspection, and record-only
resolution.

## Verification evidence

This table retains representative evidence, not every run. Superseded detail
remains in Git history; temporary results are not durable. All research
fixtures were disposable, and Zotero testing made no live-library write.

| Evidence class | Latest recorded result | Interpretation |
| --- | --- | --- |
| Editor implementation | On 2026-07-22 the locked toolchain passed typechecking and 124 WebEditor tests. Twelve cross-runtime fixtures cover exact spans, malformed syntax, embeds, BOM/newlines, Unicode, CJK, and RTL. Workload tests keep index cardinality fixed and bound 1,000 same-paragraph selections to the old/new 2,000-UTF-16 neighborhoods without document/tree rebuild. Focused Swift/WKWebView journeys cover projection, activation, shared geometry, bridge v5 recovery and exact UTF-16 reveal without source/undo mutation, one-shot Read restore, retained state, semantic scroll, 100,000-character CJK editing, and nine adaptive-grid scenarios. XCU could not target the virtualized 100,000-character AX node, so that UI edit did not occur. | Automated correctness and computed-style evidence only; not human visual, assistive-technology/installed-IME, measured visible-performance, or release acceptance. |
| Appearance line width | On 2026-07-22 data and integration tests passed the 72ch default, 48/96 boundaries, finite normalization, legacy JSON, save/reload, duplicate, shared Read/Live geometry, Source exact-type retention, 200% text, Aqua/Dark/Increase Contrast, local overflow, retained editor state, and semantic scroll-anchor contracts. One isolated XCU journey then passed real pointer resizing at 1380/1180/1080/900/720 widths, Inspector hidden/visible, Read/Live/Source switching, localized slider exposure, right-arrow 72→73 adjustment, manifest save, and nine screenshots. | Automated and bounded screenshot evidence only. The researcher still owns final acceptance of the 72ch default; genuine spoken VoiceOver, physical Full Keyboard Access, installed IME, and release/runtime system-setting acceptance remain open. |
| Repository verification | The latest 2026-07-27 `verify.sh` run passed 124 editor tests; 526 Core functional tests plus 3 isolated performance tests; 116 Contracts, 144 Application, 12 local Agent bridge, 1 serialized architecture measurement, and 316 App tests; 5 real CLI lifecycle tests; the sandboxed AF_UNIX probe; reproducible bundle and CLI checks; isolation/dead-path checks; public symbol-graph validation; optimized Release build; and RDF-1 v2 (800 notes, tree hash `bcef4ae9addf1dd01752cb5d21fb3309f7c487eb53b0b0b9e7df048f785997f9`). An earlier dirty-checkout arm64 packaging smoke passed resource, signing, and CLI checks. | Current-checkout headless verification and local smoke only; no clean tag, external install, human visual result, packaged performance, G7, or G9. |
| Research Action contracts | On 2026-07-25, 14 focused Contracts tests passed schema-v2 Action round-trip and Function-vocabulary exclusion, bounded and source-only deferred parameter validation, exact authority equality, Profile-revision validation, top-level and nested unknown-field rejection, duplicate/unsafe Method-resource refusal, reserved-ID invariants, unknown-version/kind rejection, and Action-only record projection. Three focused Application tests passed the default role matrix, disabled Method state, none/required feedback directives, new-Action versus retained-Fidelity expansion boundaries, successful deferred Analyze source resolution, custom role filtering/button execution, exact custom Method selection, execution-kind System composition, reproducible snapshots, and stale Profile request refusal. Existing Profile and storage suites continue to cover all seven native modules, strict schema bounds, exact binding, bundled replacement refusal, and unknown-field failure. | Headless disposable-fixture evidence for general resolution and custom Skill execution through the retained coordinator. Portable terminal records, the production Actions cutover, standing policy, and the typed request contract are evidenced separately; bridge delivery, native decision, child-phase integration, CLI cutover, philosophical adequacy, and researcher acceptance remain open. |
| Portable records and local execution | The Session 11 evidence on 2026-07-25 recorded 7 focused Contracts tests, 13 Core store tests, 7 permanent-deletion tests, and all then-current 47 Application research-operation/orchestration tests. It established the recursive portable whitelist, one-file record and Settle behavior, Local Execution v2, atomic grant-plus-completion, exact legacy byte/digest/mode/mtime preservation, identity-bound execution purge, terminal nonconversational Action records, Source Reference safety, and Critique/Fidelity repair paths. | Historical Session 11 headless evidence; the Discussion failure-closed limitation in that result is superseded by the next row, and standing policy plus typed request coordination are evidenced below. Actually-used Material reporting, bridge-delivered request decisions, Record Trash, disposable diff, real process-kill replay, multi-process stress, File Provider behavior, and researcher acceptance remain open. |
| Settled-note recovery | On 2026-07-26, 16 focused recovery-ledger tests, 22 portable-store tests, 12 Application lifecycle tests, and 5 recovery-policy tests passed. They cover exact current-Note pinning, stable-identity/fingerprint deduplication, missing-pin backfill, replaceable portable judgment, durable manifest fsync/readback, derived-row repair by one SQLite UPSERT, per-Note monotonic order under two ledger instances and reversed wall time, deletion/reconcile non-resurrection, fail-closed stale-ledger preview and deletion under ambiguous order, 10/30/50/unlimited retention, exact-ID cleanup journals larger than 64 KiB, interrupted cleanup resume, and Triptych isolation. A rename-before fault proves no portable state and removal of only the new pin; a rename-after fault proves the committed portable bytes and exact pin remain. | Automated local fault injection and disposable fixtures only. Settle is not a claim of truth; a pin proves only exact bytes. Restore browsing, user-selected individual deletion, disposable comparison, process-kill timing, File Provider behavior, and researcher acceptance remain open. |
| Unified portable Discussion | On 2026-07-25, one targeted run passed 98 tests across eight suites: 2 Contracts; 28 Core storage/permanent-deletion tests; 67 Application orchestration, CLI, delivery-parity, and lifecycle tests; and 1 App controller redirect test. They cover the bounded active schema, passage plus focal-note context, stable turn order, neutral Finish, idempotent and interrupted-pair reconciliation, stable-identity rename, duplicate-primary mutation refusal and projection suppression, deletion markers, active purge, finished participant tombstones, frozen Action pairing, legacy Function/Fidelity isolation, executable passage-first redirect, CLI reply, completion, read-only Markdown, and unchanged legacy files. Critique restart evidence additionally proves that portable staging cannot self-authorize execution: a remote machine without the local intent and source-machine staging with changed instructions remain nonexecutable, while exact staging plus its machine-local snapshot/instructions digest reconstructs only the identical Local v2 run. One disposable-TestVault XCU journey then passed passage selection, researcher turn, Close without Finish, a real packaged-helper CLI Agent reply while the sheet stayed closed, automatic Actions refresh, attributed transcript readback, explicit Finish, removal from Active Discussions, and exact Markdown-byte preservation. | Automated disposable-fixture and one bounded production-UI journey only. Genuine assistive-technology use, human visual acceptance, OS process-kill injection, multi-process stress, File Provider behavior, and the Session 19 record browser remain open. |
| Analyze source access | On 2026-07-25, 7 focused Contracts tests, 11 Core store tests, 7 Zotero Application tests, 15 Zotero request/metadata tests, and 38 Application research-operation/function tests passed. They cover safe encoding and legacy decode; balanced read-only bookmark leases; corrupt/permission repair without peer loss; ancestor/leaf symlink, hard-link, nonregular and revision refusal; localhost-only no-redirect Zotero identity; missing-source and legacy-run delivery/completion refusal; transient-path escaping; nonempty Zotero-key drift; deletion cleanup; and corrupt-store deletion preflight. The Session 13 App controller test additionally passed disabled-but-repairable source presentation, local binding, exact Action re-resolution, and preparation remaining blocked until access became available; the disposable production journey opened the real source module from an initially unbound Analysis. | The Open panel itself was not completed in the automated journey, so packaged-sandbox bookmark creation/reopen and the Zotero attachment chooser remain open, together with human visual/accessibility acceptance and intellectual adequacy. |
| Production Research Actions | On 2026-07-26, the retained Session 13 focused run recorded 70 passing tests: 13 Profile contract tests and 57 App controller, Settings-draft, localization, retained-Function, and source-reachability tests. They cover strict source/Profile agreement, exact role/action projection, stale Target and late dependency rejection, disabled-but-repairable Analyze, declarative parameter capture, passage parity, launcher-versus-sheet resolution isolation, load-failure Profile disposal, per-run late preparation/cancellation recovery, in-flight replacement refusal, no-Target recovery reachability, cleanup barriers, focus routing, localized recovery copy, and absence of activity chronology. Interface localization validation passed for English and Simplified Chinese. A final disposable-TestVault XCU result (`Test-ScholiumUITests-2026.07.26_22-23-55-+0800.xcresult`) retained 4/4 passing journeys: categorized Research Guidance with retention-policy persistence; Analysis, Topic, and Work Action order through pointer, keyboard, focus, accessibility structure, menu parity, and minimum width; ordinary-prose passage Discussion through Close, attributed reply, reopen, and Finish; and the Actions/common sheet in named light and dark appearances. | Automated fixture and accessibility-tree evidence only. Genuine spoken VoiceOver, physical Full Keyboard Access, installed IME, Increase Contrast, Reduce Transparency, Reduce Motion, and researcher visual acceptance remain open. |
| Quiet Comment and Action continuity | On 2026-07-27 the locked editor toolchain passed typechecking and all 124 WebEditor tests. A later mode-boundary repair passed 66 focused Frontend and real-WKWebView tests: Edit's footnote widgets no longer install footnote-specific pointer handlers, native pointer input hands ordinary caret placement back to CodeMirror without changing source bytes, and Review hover, definition navigation, and occurrence-specific focus-restoring return all execute against the sanitized projection. A new focused XCU journey compiled but macOS timed out while enabling automation before its test method began. The bounded fallback used one QA app and disposable TestVault through the accessibility API: pressing Footnote 1 scrolled to its definition, Return restored focus to Footnote 1, Edit exposed no footnote navigation buttons, and Source exposed only its exact text area; the app, fixture, and state were then removed. Earlier focused Swift/WKWebView and two passing disposable-TestVault XCU journeys covered the Review-only Comment boundary, line-only range persistence, Edit formatting without Comment, Review/Edit/Source labels, restrained secondary-click menu, Target-bound Actions, and the complete Review Comment → Discuss → external Agent reply → reopen → Finish path while preserving exact Markdown bytes. The latter uses a Debug/QA-only submit control because XCU does not synthesize the DOM Return event; the production composer still implements Return to save, Shift-Return for a line break, and Escape to cancel. | Automated and bounded QA evidence only. Human visual approval, real Return-key use, genuine spoken VoiceOver, physical Full Keyboard Access, installed IME behavior, Increase Contrast, Reduce Transparency, Reduce Motion, and the complete appearance/adaptation matrix remain open. |
| Research workflow interface proofs | On 2026-07-25, 5 focused source-architecture tests passed proof/state completeness, exact role-specific Action order, shared Skill-fixture semantics, app-owned fields, appearance/accessibility entries, forbidden material APIs, and QA-only routing. That run's responsive Research Record proof is historical and was retired when the researcher selected one fixed 760 × 680 secondary utility layout in Session 19. A separate bounded Computer Use inspection exercised every state and exposed a read-only System Skill row that incorrectly offered Edit; the corrected proof removes that action. | Static synthetic interface evidence only. Named light/dark, Increase Contrast, Reduce Transparency, Reduce Motion, and 200% entries are not equivalent to researcher visual acceptance, genuine spoken VoiceOver, Full Keyboard Access, installed-IME, or production integration. No proof establishes that a real Skill can be installed, resolved, authorized, executed, or recorded. |
| Researcher Skill installation | On 2026-07-25, 14 focused Core tests passed bounded path/content-free staging inventory, automatic capacity release after private staged-byte expiry, exact UTF-8 BOM resource round-trip, independent disabled installation, unchanged Working Method bindings, network/non-directory/archive refusal, linked-directory and linked-file refusal, nested/path-shaped resource refusal, precise unsupported-root-entry diagnosis, source and post-publication executable/hard-link refusal with typed public errors, invalid UTF-8, the early 129-file ceiling, per-file and total-package oversize refusal, malformed metadata, all-destination collision preflight, dangling v2 and executable retained citation/bibliography binding refusal while ignored legacy Function guidance does not block, cross-destination active-path rollback, preservation of late descriptor writes in hidden recovery quarantine, and recovery-required reporting for identical-byte replacement, package movement, and Skills-root replacement. One focused Application runtime test installed the same reviewed preparation into two disposable Triptychs and reopened both exact independent package revisions. | Headless disposable-fixture evidence for the backend boundary. The staged production Settings route is now reachable and is recorded separately below, but recovery quarantine still has no production reveal/cleanup interface and no packaged-sandbox Open Panel journey, general Action execution, or philosophical-quality claim is included. |
| Research Guidance Settings | On 2026-07-25, 15 Contracts tests passed the complete bounded Profile contract plus deterministic storage, key/bundled-replacement/unknown-field refusal; 6 Core Profile/Settings tests passed revision-checked save/reopen/replace, explicit independent Triptych copying, displaced-control uncertainty, fail-closed invalid state, bound-Skill deletion refusal, exact recoverable deletion, and direct activated-Manuscript editing; 4 App draft-state tests passed contract validation, all seven modules, Triptych/package/Action draft isolation, root-owned navigation persistence, and discard to the latest saved source; 14 Settings architecture tests passed the single categorized production surface, bounded editing/installation reachability, cancellation and active-Triptych reload checks, and stale New Local Skill dismissal; and 16 adjacent Core Skill-store plus 38 Working Method tests passed. Interface localization validation passed with English and Simplified Chinese entries using the established “脉络” product term. Two serial disposable-TestVault XCU journeys passed all five categories, explicit Working Method installation, seven method rows, disabled hidden Manuscript, non-authorizing Permissions text, Sources and Recovery routes, local Skill creation, Profile creation, deliberate Show in Actions, portable file creation, and persistence after Settings reopen. | Historical Session 9 configuration evidence; the Permissions placeholder in that journey is superseded by the next row. Bundled philosophical-method quality, packaged-sandbox Open Panel installation, genuine spoken VoiceOver, physical Full Keyboard Access, installed IME, and researcher visual acceptance remain open. The legacy Settings implementation is unreachable but remains compiled until Session 22. |
| Standing research permissions | On 2026-07-26, 7 Contracts tests passed stable policy values, complete deterministic Skill/Profile envelope digests, inheritance, invalidation, Work escalation, nonredundant initial Action authority, strict fields/versions, and malformed requests. Seven Core tests passed missing-state default, private atomic save/reopen, independent-instance consistency, serialized concurrent writes, stale-revision refusal, override removal, and corrupt, cross-Triptych, linked, or over-permissive failure closure. Two Application disposable-fixture tests passed all three policies, exact current-subject derivation, Ask Only for Works, initial explicit Action authority, independent runtime consistency, stale write refusal, Skill-revision invalidation, no broad-default fallback, stale caller-digest refusal, and rejection of an override approval made against a superseded UI envelope. Fifteen Settings architecture tests passed the categorized production route and Bootstrap statement; English and Simplified Chinese localization validation passed. A disposable-TestVault Computer Use journey installed Working Methods, exercised the native three-choice Triptych policy, saved one per-Skill Triptych-wide override, navigated away, reloaded both persisted values, and visually exposed and then verified the corrected readable Action names. | The focused XCU journey was attempted twice, but the macOS test runner timed out while enabling automation mode before the test method began; this is not passing XCU evidence. Computer Use supplied visible pointer and accessibility-tree evidence under default appearance only. Genuine VoiceOver, Full Keyboard Access, light/dark, Increase Contrast, Reduce Transparency, Reduce Motion, installed IME, multi-window production UI, and researcher visual acceptance remain open. The policy layer is not a child-phase grant and does not police external agents or network activity. |
| Agent change request coordination | On 2026-07-27, the earlier 6 Contracts and 6 Core tests retained strict encoding, replay, bounds, expiry, one-unresolved-request, isolation, and deletion-cleanup evidence. The expanded Application fixture passed manual subset resolution, stale-on-live-Note-change refusal, recovery of a researcher cancellation after the parent cancellation alone committed, and Triptych-wide standing-policy auto-resolution after complete Action/Skill/Profile/identity validation. Six App coordination tests passed exact-Triptych key-window choice, key-window priority after a background window becomes available, one claim across replay and show, closed-window rerouting, cached-expiry refusal, and stale-versus-terminal presentation behavior. One disposable TestVault XCU journey opened the production native sheet through a QA-only synthetic request, exposed the requested authority, all three decision actions, Agent reason, and requested Notes, and dismissed through Escape/Continue Without Changes while retaining the Workspace window. | The concurrent deletion attempt remains nondeterministic safe-order evidence rather than a scheduler proof. The synthetic UI fixture exercises production presentation but not bridge authentication or a durable decision; headless tests own those claims. Process-kill replay, File Provider behavior, genuine VoiceOver, Full Keyboard Access, installed IME, and researcher visual acceptance remain open. |
| Permission-bound continuation | On 2026-07-27, 8 focused Agent request Contracts tests, 1 strict lineage test, 6 Core request-store tests, 15 checkpoint tests, 12 local-bridge tests, 3 existing Application request/decision/key tests, and 2 new Application continuation fixtures passed. The continuation fixtures proved exact subset creation, byte-identical parent execution state, single-Target child envelopes, idempotent child IDs/grants, parent cancellation refusal before preparation, delivered-child completion after parent cancellation, cleanup and retry of interrupted partial preparation, live Note/Skill/Profile change refusal, partial sibling completion, child final-revision Fidelity, conflict-safe stale-save refusal, cancellation with retained exact-Note checkpoint, selective exact-byte restore, refusal of generic continuation construction and sibling-path restore, more than 10 simultaneous child checkpoints without ordinary automatic-checkpoint pruning, schema-v1 read-only bridge compatibility, portable lineage, reopen lineage without a recovered plaintext activity key, Analyze to Synthesize, Critique to Write, and activated Manuscript to Write. | Headless disposable fixtures establish current local behavior, not process-kill crash replay, cleanup-interrupted-again fault injection, File Provider behavior, external-agent quality, accessibility, researcher acceptance, or release readiness. Lineage is correlation evidence only. |
| Scholarly Research Record window | On 2026-07-27, 3 focused App model tests passed deterministic 5,000-record indexing, canonical-Unicode search, compound Note/date/Skill/Action/participant filters, tombstones, stable ordering, and pin refresh; 1 focused Core store test passed exact non-pin field preservation. The complete repository verification then passed 124 WebEditor tests and 1,122 Swift tests across Core, performance, Contracts, Application, bridge, architecture, and App products, plus the Release build and repository scripts. Independent read-only review found no P0/P1 issue; stable-identity deep links, failed-navigation cleanup, no-Target availability, empty This Note scope, open-time date refresh, and the fixed no-Sidebar utility presentation were corrected during review. | The disposable production XCU journey built successfully but the macOS runner timed out while enabling automation before the test method began, so it supplies no passing UI evidence. Production UI coverage for a nonconversational Action, tombstoned/cross-Note records, deep-link navigation, focus transfer between distinct Triptychs, and real assistive technologies remains open. Concurrent pin-versus-tombstone stress is also not directly exercised. |
| Local Agent request bridge | On 2026-07-27, focused Contracts/Core tests proved exact-parent-bound digest-only coordination grants and absence of plaintext keys from Local Execution v2. Twelve bridge/protocol tests passed JSON-RPC error classification, current-UID delivery, private modes, exclusive ownership, linked-directory refusal, malformed and oversized frames, idle timeout, App-closed unavailable behavior, subsecond record round-trip, cooperative cancellation, client deadline uncertainty, cancellation-insensitive fail-closed listener shutdown, bounded stop reporting, schema-v1 allowed-record readback without implicit continuation, exact schema-v2 child-delivery validation, and a filesystem secret scan. One Application fixture rejected a wrong key, a copied cross-run grant, and reuse for a second request ID before completing authenticated submit, exact replay, status, idempotent cancel, and persisted-key absence. One App composition fixture retained a typed overlong-socket-path startup failure while the ordinary runtime remained usable. The executable MCP test passed initialize, exact three-tool discovery, stdout-only framing, and structured typed unavailable without constructing an ordinary CLI snapshot runtime. A separately signed Debug probe used a unique marker-guarded bundle/container identity, the selected macOS 27 SDK, production App Sandbox entitlements, a nonsandboxed bundled CLI, and disposable state; the CLI reached the App-owned AF_UNIX socket with directory 0700/socket 0600, while the prelaunch request returned unavailable. Schema-v2 bridge results now deliver validated independent child packets only when their run, Target, checkpoint, authority, and lineage match the allowed correlation plan. | Transport, protocol, and bounded machine-local security evidence only. The probe used ad hoc signing and a Debug binary, not a release package, Developer ID, notarization, or researcher data. macOS may retain only its own empty identity marker for the unique probe bundle after marker-validated cleanup. Adversarial different-UID peer, process-kill mid-request proof, File Provider behavior, and researcher acceptance remain open. |
| Product Skills | On 2026-07-24, 5 focused Contracts-suite tests passed, including binding-v2 state round-trip and unknown-version/ambiguous-state refusal; 38 focused Core tests passed default installation, edit, disable, replacement, protected Function matching, full-package bundled restore, reopen, malformed/missing refusal, linked-control refusal, hidden-bootstrap residue and stable-collision preservation, legacy byte/mode/mtime canary, interposed package/binding changes, post-binding-commit recovery, transitive dependency/cycle/Method-dependency rejection, one exact Method per Action packet, binding/primary/Practice assembly races, exact resource capture, System attachment, citation composition, maintenance snapshot restore, forced cross-volume recovery with retained-stage isolation, and the no-rollback boundary after archival begins. Twenty-six focused Application orchestration tests and 6 derived-refresh tests passed new-Triptych bootstrap, established-Triptych noninstallation plus explicit repair, Working Method packet use, legacy-guidance exclusion, optional Manuscript activation through v2, and existing completion/Fidelity behavior. The forward prompt/evaluation specification remains unexecuted. See [package architecture](../ScholiumCore/Resources/Skills/README.md) and [evaluation guide](../ScholiumCore/Resources/Skills/evals/REAL_WORKFLOW_ASSESSMENT.md). | Executable structural and orchestration evidence for binding v2 plus headless Profile execution, not process-kill crash replay, malformed-v2 repair, retained-portable cleanup, philosophical quality, Method fidelity, live request delivery/decision, field trials, or manual acceptance. G10 and J-014–J-016 remain incomplete. |
| Protected Functions and CLI | Focused Contracts/controller/Application/installer/Settings tests and the executable lifecycle passed Discuss contracts and researcher Finish, selected-passage Comment reply/Finish, exact-current-Target Analyze/Synthesize/Write preparation, multi-target grant rejection before checkpoint, per-note Fidelity compatibility, Critique dispositions and Complete Round, routing, installation, strict parsing, function availability, promotion, cancellation, bibliography, transports, and declared failure cases. | The production macOS UI now enters through Action identity and delegates protected execution into this retained coordinator. CLI syntax, compiled Function-era presentation source, child-phase requests, and philosophical adequacy remain open. |
| Search and derived state | Search v4 passed parser/projection/Unicode/CJK spans, explicit rejection of the removed `status:` and `review:` fields, diacritic-folded lexical matching with exact source recovery, finite syntax diagnostics, one-corpus ranking, 10,000-collision exact identity, atomic generation/cancellation/recovery, Saved Search migration, GUI/CLI parity, stale navigation rejection, Related isolation, and exact editor reveal coverage. Under tokenizer policy v2, the latest 2026-07-27 full-verification load's fixed 2,056-note mixed-script fixture on Mac16,12 / macOS 27.0 / Xcode 27 beta recorded warm-query p95 74.905542 ms, incremental-publication p95 17.147292 ms, cold rebuild 732.593041 ms, 52,599,752 database bytes, and 151,764,992 peak process RSS bytes. Earlier focused disposable XCU passed four-level ranking reasons, isolated direct Related navigation, and keyboard selection/open; captured screenshots show the implemented full-width warm row and accent leading rule, plus an untinted retained workspace and native toolbar behind the opaque Search command surface. A source-architecture regression enforces the transparent outside-click dismissal layer without a visible scrim. | Core thresholds and focused automated UI evidence passed. Screenshot inspection is not researcher visual acceptance. The 30-sample GUI lexical first-paint protocol, disposable complete Search UI matrix, human ranking review, pointer, visual, VoiceOver/Voice Control, Dictation, and real CJK IME acceptance remain open. |
| UI journeys | The 2026-07-23 one-build, serial Complete run predates D-105. Its retained raw result enumerated 72 enabled journeys plus 3 explicitly disabled specialized journeys and recorded 49 passed, 22 failed, and 1 skipped without automation or infrastructure timeout. Dirty-editor termination recovery, dirty external rename, dirty Live save/switch, and the 100,000-character CJK journey passed, and no Save Failed recovery sheet was observed. On 2026-07-24, one isolated Session 3 Work journey passed the current Research menu and complete Actions order, hidden Manuscript boundary, Critique, Discussion Target and Materials search/select/remove, all five optional feedback modules plus Academic Outcome, exact-current-Note Write boundary, Fidelity controls, and keyboard dismissal. | The focused Session 3 journey used a disposable TestVault and synthetic accessibility/pointer/keyboard automation only; it did not establish post-dismiss initiating-control focus restoration. The old Annotation journey and Annotation-related failures are no longer applicable and were removed with the product surface; therefore the historical 49/22/1 result is not a current baseline. A fresh disposable Complete profile must establish the next unique baseline. Human visual, genuine spoken VoiceOver, installed-IME, packaged-performance, upgrade, and end-to-end acceptance remain open. |
| Upgrade safety | `verify-qa-upgrade-safety.sh` passed distinct baseline `587b93a0a710bac273601c1fa86ad4db1578702c6cf34884f525c3fb8df8000c` and candidate `b7b3e109cb37e83e4dd3d6c76dac26f04abac1fed019bf85ef518a446480e551` builds; all 195 vault files retained path, size, SHA-256, permissions, and mtime, with only allowlisted manifest/identity changes. | Evidence for those QA builds only; rerun every release. Not installed-app or private-vault evidence. |
| Performance | On Mac16,12 / macOS 27.0 / Xcode 27 beta, the latest RDF-1 diagnostic retained the same tree and recorded the complete cold builder at 910.42 ms; add/edit/rename/delete were 162.74–174.87 ms. A 100,000-CJK Search projection completed in 214 ms with exact UTF-16 recovery intact. The retained three-sample Debug warm-library journey reached all 267 rows in 3,583.66 / 1,533.85 / 1,562.20 ms; sample zero includes variable OS/Accessibility cold-launch cost. | Actionable regression and scenario evidence only, not p50/p95 or G7. The retained Debug samples still exceed the proposed 1,000 ms launch limit; memory convergence, approved thresholds, and the exact clean-tag packaged Release 5-warm-up/30-sample run remain open. |
| Application icon | On 2026-07-22 the approved 1,254 × 1,254 source was scaled without cropping or recomposition into ten sRGB `.icns` representations from 16 × 16 through 1,024 × 1,024. Round-trip extraction passed; `CFBundleIconFile` and debug, QA, and release scripts reference `ScholiumIcon.icns`. The current icon SHA-256 is `5ac0af473759927b027b930aa8398e192ff17899d297bf45972f1ba3b56b7f42`. | Deterministic file and resource-reference evidence only. No app was launched, and Finder, Dock, system appearance variants, clean-account installation, and packaged-Release visual acceptance remain open. |
| Distribution | A 2026-07-20 local arm64 Release smoke passed metadata, licenses, Editor resources, inside-out app/helper signing, strict signature verification, CLI execution, ZIP, and checksum. Provenance recorded `source_clean=false`; there was no exact tag and signing was ad hoc. | Local preflight only; not clean-tagged G9, external-install, or release-asset evidence. Developer ID/notarization is not required by the selected source-first Beta channel. |

`BETA_RELEASE.md` fixes the source-first `v0.1.0-beta.1` policy: exact
`GPL-3.0-or-later` source plus optional ad-hoc-signed app ZIP and checksum.
Bundle metadata and packaging align, but no clean tagged artifact, external
install, or public release has passed. Developer ID/notarization remains a
future channel choice, not a current Beta acceptance gate.
